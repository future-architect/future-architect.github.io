# NUL が紛れ込んでも diff が binary 扱いにならないようにする
*.js diff
*.md diff
*.ejs diff
*.styl diff
*.yml diff

# 記事は LF に統一する。CRLF が混ざると一括変換のスクリプトで扱いを誤る
# （挿入した改行が \r\r\n になる、git show の読み方で全行が違って見える 等）
*.md text eol=lf
