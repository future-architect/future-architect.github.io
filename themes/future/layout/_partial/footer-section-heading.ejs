<%# 記事末の自動生成ブロックの見出し (#2874)。本文の節ではなくサイトが足した
    ブロックなので、section-heading（本文 h2 と同じ大きさ＋押せるアンカー）は使わない。
    アンカーを持たないのは、目次に載らない見出しに本文と同じ作りを持たせないため %>
<h2 id="<%= id %>" class="footer-section-header"><%= text %></h2>
