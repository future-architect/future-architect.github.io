# CLAUDE.md

フューチャー技術ブログ（https://future-architect.github.io ）のソースリポジトリ。
Hexo 7.3 製の静的サイトで、GitHub Pages にホスティングされている。

## 構成

| パス | 役割 |
| --- | --- |
| `source/_posts/<年>/` | 記事本体（Markdown）。年ごとのディレクトリに分かれる |
| `source/images/<年>/<記事ID>/` | 記事ごとの画像。`thumbnail.jpg` / `top.jpg` を置くのが慣例 |
| `source/_mermaid/` | mermaid 図のSVGキャッシュ（`make mermaid` が生成。手で編集しない） |
| `themes/future/` | 自作テーマ。`layout/*.ejs`（EJS）と `source/css/`（Stylus） |
| `scripts/` | Hexo の generator / helper 拡張（著者ページ、タグ、SNSカウント、OGPプレビューなど） |
| `scaffolds/` | `hexo new` のテンプレート |
| `_config.yml` | Hexo 本体設定（permalink、alias によるURLリダイレクト等） |
| `themes/future/_config.yml` | テーマ設定（メニュー、GA4 プロパティ、SNSリンク） |
| `_profile.yml` | 著者プロフィール（about / twitter_id / github_id） |

コードの置き場所は **URL ではなく部品の種別**で決める。
`layout/` 直下＝ページ種、`_partial/`＝共有部品、`_widget/`＝サイドバー、`scripts/`＝機能単位＋`lib/`。
URL は読者向け、コード配置は保守者向けの分類なので、鏡合わせにすると URL を変えるたびにコード移動が要る。
`/specials/` のような名前空間を切っても `layout/specials/` は作らない。
同じ系統のレイアウトが3〜4本に増えて見通しが悪くなったら、そのとき分ける（#2369）。

**共通化されている部品は `/specials/gallery/` が実物で並べる**（#2985）。`/doctor/` と同じ
運営用のページで、`head.ejs` で noindex にし、`/specials/` の一覧にも載せない。

- **見本は本物の partial を呼ぶ。** 作り直した見本は腐るので置かない
  （スタイルガイドが色を `_variables.styl` から引くのと同じ流儀 #2856）
- **`docs/ui-patterns.md` のような文書は作らない。** 判断の理由は CLAUDE.md が持っていて、
  md をもう1本立てると同じことが2箇所になる（#2768「一致していることを主張しない」）
- **並びは 要素 → 部品 → かたまり。** 小さいものから組み上がっていく順で、
  デザインシステムのカタログの通例に合わせる
- **台帳に並べるのは見本を持つ部品だけ**（いま20件）。テンプレートの全 partial を
  出す場所ではない。ページの骨格や `head` のような見た目を持たないものまで並ぶと、
  **「共通の部品はどれか」が読み取れなくなる**
  - 台帳は `scripts/ui_gallery.js` が `themes/future/layout/**` を舐めて出す。
    ギャラリーが呼んでいれば見本あり、`NOT_SHOWN` に理由があれば対象外、
    **どちらでもないものだけ**が「見本も理由も無い」として警告に出る。
    部品を足したら、見本を出すか理由を書くかのどちらかになる
  - **理由を書いた部品は、ギャラリーが呼んでいても対象外で確定させる。** ページ自身の
    骨格（パンくず・サイドバー）も `partial()` で呼ぶので、呼ばれたかどうかだけでは
    見本と骨格を区別できない
  - 呼び出し元が0の部品も警告に出す（いま1件: `_partial/featured-post`）
- **部品を作るかどうかの基準**（#2985）: **2画面以上**に出る／差分が**引数3つ以内**で
  吸収できる／**同じ意味**の部品である。**「将来使いそう」は理由にしない。**
  見た目が似ているだけの偶然の一致もまとめない
  - **EJS の partial は中身を差し込むスロットを持たない。** ページの骨格のような
    「包む部品」は作れないので、規則として書く側に回す
  - **引数に `layout` という名前を使わない**（#2999）。Hexo がレイアウト名として
    持っている予約語で、`partial(name, {layout: 'row'})` と渡しても**届かない**。
    エラーにはならず既定の枝で描かれるので、**生成 HTML を見るまで気づけない**。
    見た目の違いを渡す引数は `variant` にする

**ページの骨格は `container > breadcrumb > row > main#main.col-md-9.blog-posts >
aside.col-md-3.blog-sidebar`**（#2995）。サイドバーを持たないページは
`main#main.margin-top-30`。

- **`col-xs-*` は書かない。** Bootstrap 5 に無く、`site.css` にも定義が無い。
  11箇所あったが1つも効いていなかった
- **`col-sm-12` も足さない。** `.row > *` の既定が `width: 100%` なので、
  `col-md-9` だけで 768px 未満は全幅になる。`.row` の直下で明示が要るところ
  （`_partial/archive-post.ejs`）は `col-12`

## 記事の書き方

ファイル名は `source/_posts/<年>/YYYYMMDD<postid>_<タイトル>.md`。
`postid` は同日複数投稿を区別する `a`, `b`, `c`…。公開URLは `/articles/YYYYMMDD<postid>/`。

フロントマターは以下の形式（キーは Hexo 標準の `tags` / `categories` と複数形）:

```yaml
---
title: "Go 1.27 リリース連載： uuid"
date: 2026/08/04 00:00:00
postid: a
tags:
  - Go
  - Go1.27
  - UUID
categories:
  - Programming
series: "Go 1.27 リリース連載"
thumbnail: /images/2026/20260804a/thumbnail.jpg
author: 武田大輝
lede: "Go 1.27 で標準ライブラリに追加されたuuidパッケージを扱います。"
---
```

- `categories` は既存の語彙から選ぶ。使用実績（記事数）: Programming 327 / Frontend 134 / DevOps 133 / Culture 129 / DataScience 100 / Cloud 75 / Infrastructure 70 / DB 69 / IaC 57 / Mobile 57 / IoT 52 / Business 49 / Management 48 / DataEngineering 45 / AIDD 44 / Security 36 / 認証認可 24 / VR 20
  - `AI` は `AIDD` に、`Design` は `UI/UX` タグに統合済み。`_config.yml` の `alias` で転送している
  - 基盤系4カテゴリの境界（#2057 / #2461）: **基盤をコードで書く話は `IaC`**（Terraform / Ansible / CDK 等）、
    **マネージドサービスそのものの使い方・設計は `Cloud`**（S3 の署名付きURL、VPC Endpoint の要否、Pub/Sub の概念）、
    **ネットワーク・OS・ミドルウェアは `Infrastructure`**（オンプレ含む）、
    **作った後を回す仕組みは `DevOps`**（CI/CD・コンテナ・監視・保守運用）。
    `Cloud` に入るのはクラウドのサービス自体が主題のときだけで、
    クラウドの上で何をしたかが主題なら従来どおりその主題のカテゴリに置く
    （Terraform で書いた → IaC、データ基盤 → DataEngineering、CI/CD → DevOps）。
    どのクラウドかは場所であって主題ではないので、AWS / GoogleCloud の別はタグが担う
  - AIDD と DataScience の境界（#2293）: **生成AI・LLM を道具として使う話は `AIDD`**
    （アプリ・エージェント開発、プロンプト、活用Tips、社内展開）、
    **モデル・データそのものが主題なら `DataScience`**（モデル開発・学習・評価、NLP研究、学会・論文、データ分析）。
    LLM を「作る」話（基盤モデル構築）や LLMOps・実験管理は DataScience 側に置く
  - **全件を見せるときは5つの群に束ねる**（#2908）。所属と群の並びは
    `source/_data/category_groups.yml`、群の中の並びは `category_groups()` が持ち、
    **ヘッダーのドロップダウンと `/categories/` の2箇所で同じ群・同じ並び**になる。
    18件を一列に並べると行き先を選ぶのに最大18行を走査することになるが、
    群に分ければ2〜6行で済む
    - **サイドバーは群を出さない。** あそこは上限8件の抜粋なので、束ねる相手が
      群あたり2〜3件しか無く、ラベルのぶんだけ場所と手数が増える。
      **群が効くのは全件を出す場所だけ**（走査を18行から2〜6行に落とす話なので、
      8行の並びには効かない）
    - 群は **開発**（Programming / Frontend / Mobile / IoT / VR）→ **基盤**
      （DevOps / Cloud / Infrastructure / IaC / DB）→ **AI**（DataScience /
      AIDD / DataEngineering）→ **セキュリティ**（Security / 認証認可）→
      **ビジネス**（Culture / Business / Management）の5つ
    - **「基盤」は動かすものの群。** 上の基盤系4カテゴリに `DB` を足した5つ（#3006）。
      DB の70本を読むと**71%が「ミドルウェアを動かす」記事**（PostgreSQL の新機能・
      EXPLAIN・DynamoDB のコスト・Oracle からの移行）で、上の
      「ネットワーク・OS・ミドルウェアは `Infrastructure`」と同じ性格。
      タグでも「データ」側との語彙の重なりは薄かった（DataEngineering との
      Jaccard 0.065、DataScience は上位10に入らない）。
      セキュリティを分けているのは、4カテゴリの境界定義を崩さないため
    - **群を広げても CLAUDE.md 側のカテゴリの境界定義は動かさない**（#3006）。
      あちらは書き手が「記事にどのカテゴリを付けるか」の線で、群は読者が
      「どこを探すか」。**役が違うので片方だけ広げてよい**
    - **「AI」は元「データ」**（#3006）。古典的な機械学習も AI と呼ばれてきたので、
      `DataScience`（モデル・データそのもの）と `AIDD`（生成AI を道具として使う）の
      両方を束ねる名前として成り立つ。**群名は URL に出ない**ので改名に転送は要らない
    - **群の並びだけ本数から出さない。** 5つの群は読者にとって対等で、大きい順に
      並べても意味は増えず、並べ替えの理由がファイルの外に出てしまう。
      yml のキーの順がそのまま表示順になる
    - **群の中の並びの鍵は変えていない**（累計の多い順。#2959 の規則のまま）。
      issue #2908 の「累計順が実態と
      合っていない」（AIDD は累計15番目だが直近1年は1位）は、**直近1年順に
      入れ替えるのではなく群で解いた。** 直近1年を鍵にすると月あたり 8.8/18 件の
      順位が動き（累計順は 2.0、群に分けた累計順は 0.7）、下位12件は年11本以下なので
      1本差が順位差になる。群に分ければ走査対象が3〜6件に落ち、読者の入口が
      群の名前になるので、順位が実態を表す必要が無くなる
    - **群はカテゴリではない。** URL も記事のフロントマターも持たない見せ方だけの層で、
      記事の `categories` は今までどおりちょうど1つ。サブカテゴリの導入（#2908 の案3）は
      別の話で、ここには含まない
    - 群に属さないカテゴリはビルドの警告を出して「その他」として描く。行き先が
      登録漏れで navigation から消える方が害が大きい（`CATEGORY_COLORS` と同じ流儀）
- **`tags` は1つ以上、`categories` はちょうど1つ**。カテゴリと同名のタグ（`Security` タグと
  `Security` カテゴリ等）は付けない。語彙が重複すると、一覧から記事ページへ移ったときに
  「タグが1つ増えた」と誤認する。既にあるものはタグ側を外し、`_config.yml` の `alias` で
  タグURLをカテゴリへ転送する（#2291）
  - この3つと「`categories` の語が `source/_data/categories.yml` に登録済みか」は
    **textlint の `no-invalid-post-taxonomy`** が止める（#2706）。`/doctor/` の機械判定から
    移したもので、答えが決まっている検査は linter に置く。判断が要るもの（統合候補・
    1記事タグ）は `/doctor/` に残る。どのタグを付けるかは機械には決められないので `--fix` は無い
  - **タグの付け漏れの候補は「本文での出現回数」で出す**（#2784。`/doctor/` の
    「本文に何度も出てくるのにタグが付いていない記事」）。散文（コード・URL の中は除く）に
    12回以上出るのにタグが無い語を、直近1年の記事について並べる（窓は他の節と同じ
    `ACTIVE_DAYS`。過去すべてを出すとノイズになる）。しきい値を下げると急に増える
    （8回で109件、3回で573件）
    - **語が出る位置は見ない。** タイトル照合（#2708 で一巡して廃止）を lede と章・節・項に
      広げる案を測ったが、候補が1,493行に増えて妥当率が lede 30%・見出し 10% に落ちた。
      lede は背景を書く場所なので「A ではなく B の話」の A を拾い（GitLab の記事に `GitHub`、
      Vue.js の記事に `React`）、見出しは節の粒度なので手順の1ステップを拾う
      （「Linuxインストール」「Dockerイメージをプッシュ」）。見出しは本文の一部なので、
      章立て全体に渡って出てくる語は回数の方で自然に上がる
    - **タグの一般性では絞れない。** 「その語が本文に3回以上出る記事のうち実際にそのタグが
      付いている割合」を411タグで測ると、#2708 で付けたタグは 8〜67%、見送ったタグは
      2〜36% で重なる（`JSON` 8% は付け、`リトライ` 33% は見送っている）
    - 照合に使えない語は `tag_ontology.yml` の **`notInText: true`** で外す。理由は
      同名衝突（`インデックス` ＝連載の索引と DB のインデックス）と一般語（`Web` / `ドキュメント` /
      `GitHub` のように主題を問わず本文に出る）の2つだけ。「この記事にこのタグは付けない」という
      記事側の判断は書かない
    - 抽象タグは提案しない。記事のタグから `broader` を辿って届く語は #2292 の方針どおり
      書かなくてよいものなので外す
  - **タグの件数はフロントマターの合計と一致しない**（#2997）。`Go1.27` のような版のタグを
    持つ記事には、**語幹のタグ（`Go`）がデータ層で実タグとして注入される**
    （`scripts/version_tags.js` の同義展開 #2292）。タグページ・件数・関連記事・OGP の
    すべてが「両方書いた」のと同じになるので、執筆者は版だけ書けばよい
    - 実測: `Go` はフロントマター 262本 ＋ 同義展開 3本 = **265本**。足されているのは
      `Go1.18` / `Go1.24` は書いたが `Go` を書いていない3記事
    - **検算するときは同義展開を足してから比べる。** フロントマターだけを数えると必ず合わない
    - 対象は**バージョン・年の版違いだけ**。語幹の導出は3層（名前規則 → `versionOf` →
      `notVersion`）で、`tag_ontology.yml` の `broader` とは別物（あちらは関連記事の
      弱い接続にしか効かない）
- `author` は `_profile.yml` に未登録なら追記する。**1記事1名**（配列にしない）。
  共著は著者ごとの集計・一覧・チャートで扱いが分かれ、規則が追えなくなる
- `date` は同日に複数投稿するとき、`postid` の順に時刻をずらす（`a` は `00:00:00`、`b` は `00:00:01`）。
  完全に同じ日時だと一覧の並びが同着になり、ビルドごとに順序が入れ替わる（#2055）
- `lede` は一覧・OGP に出る概要文
- 画像は `<img src="/images/2026/20260804a/xxx.jpg" alt="" width="1024" height="559" loading="lazy">` のように実寸の width/height を明記する
  - これはリポジトリに入る形。**寄稿者には `![alt](url)` で書いてもらい、公開時にこちらで変換する**（#2559）。
    記法ガイド（`/specials/markdown/`）もそう案内している。変換漏れは textlint の
    `no-markdown-image` が止める（#2644）。`width` / `height` で表示サイズを変えるのは避け、
    小さく見せたいときは画像ファイル自体を加工する（実寸と違う値だと大きい画像を送って縮めるだけになる）
  - **縮めて書くなら比を保つ**（#2643）。比が実寸とズレると、読み込み前に間違った高さで場所が確保され、
    画像が出た瞬間にレイアウトが動く。**寸法を書いているのに CLS が出る**ので未設定より気づきにくい。
    textlint の `no-img-ratio-mismatch` が画像を実測して止める（相対1％まで許容）
  - **`loading="lazy"` は2枚目以降に付ける**（#2645）。**先頭画像には付けない。**
    `scripts/lcp_image_priority.js` が先頭画像から lazy を外して `fetchpriority="high"` を付ける
    （LCP 候補に lazy が付いていると表示が遅れる）ので、書いても消えるうえ意図が読めなくなる。
    textlint の `no-img-without-lazy` が両方向を止める（どちらも `--fix` で直せる）。
    過去記事に残っていた1,138件は #2686 で外した
    - **1x1 の画像は対象外。** アフィリエイトの計測ピクセルがこの形で、画面に入るかどうかに
      関係なく取得されることが目的なので lazy を付ける意味が無い。記事側で
      `textlint-disable` して黙らせると抑止の理由を本文に書き残すことになるため、ルール側で外す
  - **白地の画像は `class="bordered"` を付ける**（#2458）。白背景のスクリーンショットやスライドは
    本文と地続きに見えて画像の範囲が分からない。枠の色は CSS（`rule-base` = `#ddd`）が1箇所で持つので、
    `style="border:…"` を直接書かない。全画像に一律で付けないのは、枠を内蔵した画像や写真で二重線になるため
  - **キャプションは画像の直下の行を `*` で挟む**（Zenn と同じ記法、#2517）。
    `scripts/figure_caption.js` が `<figure><figcaption>` に組み替える。
    Markdown 自体にキャプションの概念はなく、`![alt](url "title")` の title はツールチップで別物。
    alt は画像が見えない人への代替テキスト、キャプションは全員向けの補足なので alt は流用しない
    - **画像の前に `▼ラベル` を置かない。** 説明はキャプションとして画像の下に書く。
      前に置くと figure に入らず、記号も移動のたびに向きが合わなくなる（#2517）
    - `▲` `↑` で前の画像を指す書き方はそのまま `*` で囲ってよい。
      画像・キャプションが前後の段落とくっついていてもフィルタが空行を補う
- **コードフェンスは「端末の画面」と「実行するコード」で選び分ける**（#2505）。
  噛み合わないと強調表示が丸ごと消える
  - **端末の画面は `console`。** プロンプト付きのコマンド、コマンドと出力が並ぶもの、
    実行結果・ログだけのものも含む。「以下のように出力されます」と書いて見せる画面は `console`
  - **実行するコードは `sh` か `bash`。** プロンプトも出力も無いコマンドの列・スクリプト。
    2つの綴りは同じ Bash として描画されるのでどちらでもよい
  - **`shell` と `terminal` は使わない。** `shell` は名前がスクリプトを想像させるが実体は
    `console` の別名で、実際に158件中78%が中身と食い違っていた。`terminal` は highlight.js に
    登録が無く、プレーン表示になる
  - **プロンプトが着色されるのは行頭の `$` `%` `>` `#` の4つだけ**（実測）。
    記号の直後に空白が無くてもよい（`$go run main.go`、`>aws dynamodb scan`）。
    `❯` `➜` `PS >` `dbname=#` は認識されず無着色になるが、誤着色は起きないので
    `console` のままでよい。**強調表示が欲しいときは言語を変えるのではなく `$` を書き足す**
  - `console` を `bash` に変えるのは、プロンプトも出力も無い純粋なコマンドの列のときだけ。
    実行結果を `bash` にすると出力がシェルの構文として着色される
  - 再発は textlint の `no-mismatched-code-fence` が止める（`console` の中身は問わない）
- **ファイル名は言語の後ろに空白で区切って書く**（#2843）。hexo が情報行を空白で切り、
  2語目以降を `<figcaption>` にする。全7,878フェンスのうち2,230がこの形
  - **Qiita 記法の `` ```go:main.go `` は使えない。** hexo は `go:main.go` 全体を言語名として
    読むので、**強調表示とキャプションが同時に消える**（プレーン表示になる）。
    下書きは Qiita 由来なので変換後に残りやすい。25件あった
  - 言語欄にファイル名だけ（`` ```requirements.txt ``）も同じくプレーン表示。13件あった
  - **直前の段落がファイル名だけの行なら、キャプションへ寄せてその行を消す**（15件）。
    キャプションと二重になる。**見出し（`#### main.ahk`）と兄弟のある箇条書きは触らない**
    （見出しは目次に出る構造、箇条書きは列挙であってラベルではない。実際
    `* backend.tf / * provider.tf / * versions.tf` の3項目目がフェンスの直前に来ていた）
  - 再発は textlint の `no-code-fence-filename` が止める。`--fix` を持つのはコロンの形だけで、
    言語欄がファイル名の形は**何語として描くかを中身から決められない**ので報告だけにする
- **インラインコードを日本語の装飾に使わない**（#2609）。UI のラベルは 「」、意味の強調は太字にする。
  Chrome のページ翻訳は `<code>` の中を訳さないので、装飾で囲った日本語は翻訳しても原文のまま残る
  - 再発は textlint の `no-japanese-only-inline-code` が止める。2026年以降の記事のみ対象
    （`legacy-articles` フィルタ。過去183件/60記事の書き換えは #2609 とは別に判断する）
  - **対象は「漢字・かな・カタカナと空白だけ」の Code ノード。** 英数字も記号も1文字も無いものに絞ると、
    `{プロジェクトID}`（プレースホルダ）・`技術選定.md`（日本語ファイル名）・`-- 成績`（SQL のコメント）・
    `string型` は自動的に外れる。全角記号を含むもの（`「ステップバイステップで考えてください」`、
    `品川区、東京都`）も外す。LLM へのプロンプトやサンプルデータの値は機械への入力なのでコードでよい
  - `` `rising` → `上昇 ↗` `` のような**対応表の値側は対象外**。コード上の文字列リテラルを指している
  - 誤検知は `.textlintrc` の `filters.allowlist.allow` に語を足すか、`<!-- textlint-disable -->` で黙らせる
- **箇条書きの項目を裸URLだけにしない**（#2910）。裸URLは hexo が自動リンクにするので、
  リンクテキストがURLそのままになる。参考リストは読者が行き先を見比べて選ぶ場所なので、
  そこに名前が無いと選べない。`- [ページ名](URL)` と書く
  - 再発は textlint の `no-bare-url-in-list` が止める。**2026年以降の記事のみ対象で、
    2025年以前の402件/144記事は対象外のままにする**（#2910。`legacy-articles` フィルタ）。
    リンクテキストは1件ずつ人が決めるしかなく、2020〜2022年に237件が集中している。
    書き換えの余地があるとしても、書いた人が記事を触るときに直せばよい
  - **`--fix` は持たない。** リンクテキストは機械には決められない。全408件の `<title>` を
    実際に取ってみると、取れたのは86%で、そのうち57%はサイト名の接尾辞付き
    （`… · GitHub` / `… - Qiita` / `… | HashiCorp Developer`）。40字を超えるものが半分あり、
    URL より長くなる。親項目や直前の地の文が既に行き先を名乗っている形もあり
    （「1. モダン化（#75266）」の子項目、「以下の issue に〜」の直後）、
    そこへ取得タイトルを流し込むと名乗りが二重になる
  - **文中に埋め込まれたリンクと、段落として置かれた裸URLは対象外。** 前者は地の文が名乗り、
    後者は据え置きの判断（#2910）。箇条書きの408件のうち115件は1項目だけのリストなので、
    この線は「参考リストかどうか」を厳密には切り分けていない。それでも箇条書きで切るのは、
    近くに名乗る文があるかは機械には判定できず、2026年の15件では誤検知が出なかったため
  - **リンクテキストがURLそのものなら `[URL](URL)` と明示的に書いた形も拾う**（1件あった）。
    ルールは Link ノードのテキストと `url` を比べていて、自動リンクかどうかは見ていない
  - 直前の段落がラベルだけの行（`ログ設計ガイドライン:`）なら、リンクテキストへ寄せてその行を消す。
    コードフェンスのファイル名（#2843）と同じ形で、残すと名乗りが二重になる
- 連載記事は冒頭で `[連載名](/articles/20260728a/) の N 本目です。` と相対リンクで親記事を参照する
- `series` は連載への所属。値がそのまま連載名として表示され、記事末に前 / 次 と索引へのリンクが出る（`scripts/series.js`）
  - **3本そろってから連載にする。** 2本では前後リンクが片方しか出ず、連載として扱う意味が薄い
  - 連載名をタグで表さない。`series` に書く。タグで表していたものは `alias` で先頭記事へ転送する（#2374）
  - 値はグループ化のキーと表示名を兼ねる。表示は「連載：<値>」なので **末尾に「連載」「企画」は付けない**（`Go1.27リリース`、`CI/CD`）
  - 年ごとに続く企画は年を付けて区別する。括弧は使わない（`春の入門祭り2025`、`秋のブログ週間2023`）。索引記事のタイトルと同じ表記になる
  - バージョン番号はタグの表記に合わせる（`Go1.27リリース`。`Go 1.27` ではない）
  - 索引記事は `series` に加えて `インデックス` タグを付ける。連載を束ねる年間企画（「2026年 フューチャー技術ブログリレー企画」など）には `series` を付けない
  - **索引記事の無い連載を作らない**（#2510）。索引が無いと連載ナビの連載名がただの文字列になり、
    全体を見渡す手段が前 / 次の送りだけになる。専用の索引を立てない連載は、1本目に
    `インデックス` タグと全記事の一覧を置いて索引を兼ねさせる。
    一覧を手書きしてよいのはこの索引記事だけで、各記事には書かない（連載ナビと二重になり、古くなる #2450）
  - 並び順は日付昇順。「N本目」は表示しない。本文が名乗る番号と日付順の位置は70連載中37件でズレており、機械が本文と違うことを言う方が害が大きい
  - **索引の表のサムネイルは日付のセルに入れる**（#2790）。`| {% thumb 20260827b %} 8/27（木） | 執筆者 | [テーマ](/articles/20260827b/) |`。
    絵柄は `scripts/thumb.js` がフロントマターの `thumbnail` から引く（関連記事・ランキングと同じ 72×48 の箱）。
    日付の上に積む形になり、日付（約65px）は箱（72px）に収まるので列は太らない
    - **専用の列は作らない。** 列を1本足すと、モバイル（本文列351px）でタイトル列が
      約200px→130pxに痩せて1行あたり1行増える。日付のセルに畳めば、日付の列が
      箱の幅（92px）になるだけで済む。索引の表7本すべてが日付を先頭の列に持つので、
      絵の位置は専用の列にしたときと変わらない
    - **タグを書くのは記事へのリンクがある行だけ。** 未公開の行（「調整中」「〜（仮）」）と
      索引自身の行（「この記事です」）は日付だけを書く。**絵が無ければ何も出さない**（空き枠も
      出さない）。予定表なので未公開の行の方が多い時期があり、空き枠を並べると来ない絵を
      待っているように見える。セルには日付が残るので、絵が無くても書き忘れには見えない
      - 行の左端を揃える必要がある一覧・ランキングは事情が違う。あちらは
        `postListItem` が空き枠に差し替える（`post-list-icon-empty`）
    - **表そのものは手書きのまま。`series` から生成しない。** 索引の表は公開済み記事の一覧ではなく
      **予定表**で、データ行714行のうち258行がまだリンクを持たない（「調整中」「〜（仮）」）。
      リンクの文字列も682件中344件が実タイトルと違い、連載名を落とした短縮テーマ名になっている
      （`go mod tidy` / 「Go 1.27のgo mod tidyの更新点」）。どちらも `series` から引けない
    - **年間企画の表には足さない**（「2026年 フューチャー技術ブログリレー企画」）。1行が連載1つで、
      リンクの列に複数年ぶんが並ぶので、行と記事が1対1にならない

新規記事を追加したら、画像ディレクトリ `source/images/<年>/<記事ID>/` も併せて作る。

## コマンド

```sh
make s      # ローカルサーバ（http://localhost:4000、ライブリロード付き。hexo-server-live）
make g      # 静的ファイル生成（public/）
make css    # public/css/site.css だけ再生成（0.3秒。CSSの確認用）
make clean  # キャッシュ・生成物の削除
make fix    # textlint --fix（source/_posts 配下）
make fmt    # markdownlint-cli2 --fix（リポジトリ自身の *.md。node_modules と .claude は対象外 #2712）
            # ＋ prettier（scripts/**/*.js と *.mjs のみ #2307）
make lint   # npx lint-staged（git add 済みの記事のみ textlint）
make mermaid # mermaid 図のSVGキャッシュ更新（Docker必須、記事の図を追加・編集したら実行してコミット）
```

記事を書き換えたら `make fix` か、対象ファイルだけの
`node_modules/.bin/textlint --fix <path>` を通してから push する。

**`hexo generate` は必要なときだけ回す。** 何も変えていなくても1分半以上かかる
（記事1,449本ぶんの処理が毎回走り、書き出し0ファイルでも同じ）。

| 変えたもの | 確認に必要なもの |
| --- | --- |
| CSS（`themes/future/css-src/*.styl`） | `make css`（0.3秒）だけ。ページの再生成は不要 |
| テンプレート（`.ejs`）・`scripts/` のヘルパー・記事 | `hexo generate` |

`_config.fast.yml` を渡す差分ビルドは効いていない。`ignore` のパターンが実パスに
1つもマッチせず、記事数も画像コピー（1.2GB）も素のビルドと変わらない。
時間の内訳は書き出しではなく Hexo 側の処理なので、除外しても縮まない。

## Lint ルール

- `.textlintrc`: `preset-ja-technical-writing` + `prh` + `.textlint/` のローカルルール。一文200文字まで。感嘆符・疑問符と弱い表現は許容
  - 誤検知は `.textlintrc` の `filters.allowlist.allow` に単語を追加して黙らせる
  - **表記統一は `prh.yml` に書いた4件だけ**（`インタフェース` → `インターフェース`、
    `プリフィックス` → `プレフィックス`、`Webブラウザ` → `ブラウザ`、掛け算の `x` → `×`）。
    `spellcheck-tech-word` は個別の変換を止める手段が無く、誤検知のたびに allowlist へ
    単語を足す運用になっていたので #2184 で外した。**辞書に無い表記は直されない**
  - **`no-mix-dearu-desumasu` は無効**（#2381）。文体は「である」「ですます」どちらでもよく、
    段落単位で切り替える書き方（数学の証明、脚注、エッセイ、分析メモ）を許容する。
    このルールは `です／ます／である` の明示マーカーしか数えず `〜だ。` `〜する。` を
    どちらにも数えないため、常体の記事を「ですます調が多数」と誤判定する。
    全記事3,186件のうち本当の混在は12件だけだった
  - **`ja-no-mixed-period` は「漢字・かな終わり」だけを見る**（#2381）。`allowPeriodMarks` に
    記号・英字・数字を並べて、それ以外で終わる行を対象外にしている。狙いは
    「文なのに句点がない」の検出であって、記号終わり（`〜、` `〜:` `〜…` `【ラベル】` 顔文字）は
    正しい書き方なので拾わせない。**`.` と `．` はあえて許可しない**（`。` への統一が正解のため）
    - **文ではない段落は対象外**（`.textlint/textlint-filter-rule-non-sentence`）。位置で3つ決めている。
      長さでは切っていない（何文字までがラベルかを決められないため）
      1. コード・表・画像・引用・箇条書きの**直前**の段落。そのブロックのラベル・導入であって
         文ではなく、句点を求めても「▼ サンプルコード。」にしかならない
      2. 箇条書きの**直後**の段落。直前の項目の続き（`* **動的リンク**` の下に説明を書く形）
      3. 箇条書きの**中**（ネストを含む）。項目に句点を打たないのがこのブログの書き方で、
         1つだけ足すと兄弟の項目と体裁が割れる
      4. 行頭が**丸数字（①②…）**の段落。Markdown の箇条書きではないが連番の項目で、
         段落なので最終行だけがルールに当たる
    - **`=` で始まる行は数式**として `allowlist` で対象外にしている。`$` で囲っていなくても数式扱い
      （`= 5 byte + (5 byte - 4 byte)` のような計算の並び）
    - 直後にコードや箇条書きが続く導入文は、末尾に `…` を付けて文の未完を示す（`この意味は…`）
    - 画像キャプション・図表ラベルは `*` で囲む。段落末が強調ノードになりルールの対象外になる
    - 本文中のラベル行（後ろに本文や見出しが続く「注意」「以上」等）は `**注意**` と太字にする
  - **`no-doubled-joshi` は2026年以降の記事だけを見る**（#2381）。全記事で4,378件出るが、
    直すには一件ずつ語順を変える判断が要り、過去記事を機械的に書き換える意味が薄い。
    `.textlint/textlint-filter-rule-legacy-articles`（ローカルの filter rule）で
    `source/_posts/<年>/` の年を見て古い記事を対象外にしている。
    `from` と `rules` を渡す作りなので、他のルールにも同じ手が使える
    - textlint は filter rule のローカルパス（`./.textlint/xxx`）を解決しないため、
      `package.json` に `file:` 依存として登録して通常のパッケージ名で参照する
    - filter rule は**関数を直接エクスポート**する。「filter プロパティを持つオブジェクト」を
      求めるエラーが出るが、実装の判定は `typeof === "function"`
    - **`allow` で9つの助詞を外している**（`に` `が` `は` `も` `で` `か` `や` `と` `でも`、#2381）。
      2026年だけで299件出ていたが、中身を全部読むと大半が**繰り返しても自然な形**だった。
      格助詞・係助詞は文の骨格で言い換えられず（`〜になり、〜になるため`）、
      列挙・並列・引用の助詞は繰り返すのが正しい書き方（`JSON Pointer か JSONPath か独自キー名か`、
      `AWSでもGCPでもローカルPCでも`、`「VPC と RDS と CloudTrail を組んでほしい」と頼めば`）。
      残った4件（`ので` `から` `には` `にも`）は実際に読みにくく、直して0件にした
    - `min_interval` は**緩める向きに使えない**。2にすると299件が861件に増える（間隔の下限で、
      上げるほど厳しくなる）。緩めるなら `allow` に助詞を挙げる
  - **`no-doubled-conjunctive-particle-ga` も2026年以降だけを見る**（#2381）。逆接の「が」の
    重複で、全記事192件のうち2026年は9件だった。直すには文を分けるか言い換えるかの判断が要り、
    `no-doubled-joshi` と同じ性質なので同じ扱いにしている
    - 直し方は3つ。**括弧の中の「が」を落とす**（`（私も30代ですが）` → `（私も30代です）`）、
      **逆接でない「が」を言い換える**（`言われるがまま` → `言われるままに`、`和はあるが` → `和はあっても`）、
      **文を分ける**（`〜強調されますが、実際に〜` → `〜強調されます。実際に〜`）
    - 逆接でない「が」（`3つが論点`、`Server が公開する`）まで数える場面があるので、
      指摘の位置ではなく**文全体を読んでどの「が」を直すか決める**
  - **`no-doubled-conjunction` は無効。代わりにローカルの `no-doubled-sentence-opener` を使う**（#2381）。
    preset のルールは**文の中で最初に現れた接続詞**を文どうしで比べるため、全記事99件のうち
    71件が誤検知だった。オプションを1つも持たないので閾値でも allowlist でも絞れない
    - **一方または両方が文中の接続詞**（46件）。`省略された項の同定および復元`、
      `新しい又は改善した製品` のような列挙の語は文の始め方ではなく、繰り返すのが正しい
      （`no-doubled-joshi` で列挙の助詞を外したのと同じ理由）。引用の中の発言
      （`「でも、俺はやれる範囲でやっていたもん！」`）もここに入る
    - **両方が文頭だが間に文が挟まる**（25件）。`また、A。B。C。また、D。` のように離れていても
      報告する（接続詞を持たない文をまたいで直前の語を持ち越すため）
    - 置き換えたルールは**隣の文の文頭だけ**を見る。読者が「同じ言葉で続けて始まった」と
      感じるのはその形だけで、残った件は実際に読みにくいので本文を直した
    - 文頭は行頭の記法（`**` や引用記号）を除いた最初の形態素で判定する。`「` は落とさないため、
      引用した発言は文頭が接続詞にならず自動的に外れる
    - **文頭を切り出すときはコードポイント単位で切る。** 絵文字の途中で切ると kuromoji が
      例外を投げ、textlint がそのファイルの走査ごと止まる（`String#slice` では踏む）
  - **`max-ten` は読点6個以上で検出**（`max: 5`、#2381）。**5個は許容する**。
    既定は4個以上を拾うが、4〜5個は自然な文も多く、直すとかえって不自然になる。
    6個以上の182件を直した結果、残りはほぼ引用だけになった
    - 直し方は3種類あり、まず型を見分ける
    - **列挙**（`記者、デスク、校閲、整理`）は中黒に置き換える。文構造は変えない。
      ただし `とか` `やら` `だったり` で既に列挙が表されているなら中黒にせず読点だけ削る
    - **言い淀みの読点連打**（`恥ずかしいですが、、、`）は三点リーダにする
    - **文が長い**場合だけ分割する。文字数も減るので `sentence-length` にも効く。
      分割で `また、…また、` と接続詞が重なると `no-doubled-conjunction` に当たるので注意
    - **引用は直さない。** 引用を含む文なら、引用部分の読点は残して外側だけ分割する
  - **`max-comma` は無効。代わりにローカルの `no-halfwidth-comma-in-japanese` を使う**（#2381）。
    `max-comma`（一文の半角カンマ3個超）は個数を見る代理指標で、139件のうち129件が
    英語トークンの羅列・英文引用・数式・参考文献だった。直したいのは
    **日本語の中で読点の代わりに半角カンマを使っている**ことなので、
    「**空白なし**で日本語に挟まれた `,`」だけを見る（`背景,共分散`）
    - 空白を挟む形（`佐藤元紀, 能地宏`、`特徴量, 抗原性`）は対象外。書誌や英語混じりの
      列挙が大半で誤りとは言えない。直すときも、周りが `, ` で揃っているなら空白を足せばよい
    - 表のセル（タグの列挙）と引用は対象外。コードは Str ノードに出ないので自動的に外れる
    - 個数を見ないので `千葉さん,織田さん,加部さん` のようなカンマ3個以下の誤用も拾える
      （`max-comma` では閾値に届かず見逃していた）
  - **`ja-unnatural-alphabet` は無効**（#2381）。日本語に挟まれた1文字のアルファベットを
    「入力ミス」とみなすルールだが、124件のうち本当の誤りは6件だけだった
    - 半角48件はほぼ**1文字の変数名**（`インスタンスvの`、`チェインの長さm`、`点pの近傍`）で、
      技術文書として正しい書き方。`allow` は文字単位でしか除外できず、
      変数名と打ち間違いは文脈でしか区別できないので、オプションでは絞れない
    - 全角76件は**固有名詞と外部表記の写し**（`Ｘ線造影剤`、`ＬＰガス`、秋月電子の型番、
      `ＫＡＤＯＫＡＷＡ`、`１ｏｎ１`）。半角に直すと引用元の表記を変えることになる
    - 拾えた誤りのうち、**掛け算の `x`** は `prh.yml` に移した（`単価x数量` → `単価×数量`）。
      前後が漢字かカタカナのときだけ当てるので、`変数xがポインタ型`（直後が助詞）や
      `スペースXドキュメンタリー`（大文字）には当たらない
  - **`no-unmatched-pair` は残す**（#2381）。括弧や引用符の閉じ忘れは表示が壊れるので、
    件数が少なくても検出力を持っておく価値がある（表がまるごと崩れていた箇所と、
    URL末尾に `(` が混入して404になっていたリンクがこれで見つかった）
    - ただしこのルールは**対を1つのテキストノードの中でしか見ない**。括弧の中にリンクや
      改行が入るとノードが割れて閉じを見失うので、誤検知の型を `allowlist` で外している
    - **括弧の中にURLを含む対**（`FutureVuls（ https://vuls.biz）`）… 裸URLが自動リンクになって
      ノードが割れる。閉じ括弧がある形だけ外れるため、本当の閉じ忘れは今後も拾える
    - **引用符そのものを説明している箇所**（`（"）`、`( " )`）とネットスラングの `(ry`
    - `allowlist` の正規表現に **`/` を書かない**。JSON では `\/` が `/` に戻り、`/…/` の
      パターンとフラグの切り出しが壊れて無言でマッチしなくなる（`https?:` だけで足りる）。
      文字クラスの改行除外も `\\n` と書く（`\n` は JSON が実際の改行に変える）
    - **改行や段落をまたぐ対は記事側を直す。** 段落内の改行は `<br>` になるだけなので、
      文の途中で切っている引用は1行にまとめる。段落をまたぐ括弧は段落ごとに閉じる
  - **`max-kanji-continuous-len` は無効**（#2381）。漢字11文字以上の連続は全記事54件・18記事あり、
    そのほぼ全てが**固有名詞**だった。`情報処理安全確保支援士`（12件）、`個人情報保護士認定試験`、
    `全日本情報学習振興協会`、`北陸先端科学技術大学院大学`、`電気機械器具防爆構造規格`、
    `岡崎市立中央図書館事件` のような資格名・団体名・規格名・判例名には読点も送り仮名も
    入れる場所が無く、崩すと別の名前になる
    - 既定の5文字から10文字に緩めた上で残ったのがこれなので、閾値では切り分けられない
    - `allowlist` に足す手は取らない。資格名・団体名は記事が増えるたびに新顔が出る
      （合格体験記が続く限り増える）ので、いたちごっこになる
    - 直す余地があったのは `障害発生時対応方法説明` のような複合名詞の積み上げ2件だけで、
      ルールを残して固有名詞を毎回黙らせる手間に見合わない
  - **`no-double-negative-ja` は無効**（#2381）。22件すべてが**意図的な緩和表現**で、
    肯定に直すと意味が変わる（「読めないことはない」＝かろうじて読める。「読める」ではない）。
    `〜ないわけではない` 7件・`〜なくもない` 4件・`〜なくはない` 3件が中心で、
    出るのは感想・随筆の段落。2026年の記事には1件も無いので、年で絞る手（`legacy-articles`）も効かない
    - 拾えた実誤りは `書けないわけではないありません`（二重否定の直しかけ）の1件だけで、これは直した
  - **`ja-no-redundant-expression` は dict6 も無効**（#2381）。`[サ変名詞]を実行` を
    `[サ変名詞]する` に直す辞書だが、IT の文章の「実行」は**「走らせる」の意味の他動詞**。
    `命令を実行する` → `命令する`、`サービスを実行する` → `サービスする` は別の意味になる。
    11件すべてが誤検知で、`Do()を実行` のようにコードの断片も拾っていた
    - **dict5（`〜を行う`）は残し、既定の allows に長音符を足した。** 既定は
      `/^[ァ-ヶ]+を.?行[ぁ-ん]/` で `ー`（U+30FC）が文字クラスに入っておらず、
      「フィードバックを行う」「アプローチを行う」が漏れていた。`K-means++` のような
      名前も「する」を付けられないので、日本語以外で始まる形も許可に加えた
    - **dict1 / dict2 は `allowlist` で `〜に／とすること〜できる` を外す。**
      29件のうち15件がこの形で、「する」は「〜にする」の一部なので省けない
      （`データセットにすることもできます` の「する」を取ると文が壊れる）。
      省けるのはサ変名詞に付いた「する」だけ（`管理することもできます` → `管理もできます`）で、
      それは2件だった。辞書は `することが` しか見ておらず、直前が助詞かサ変名詞かを区別できない
  - **`ja-no-successive-word` の allow に口語の反復を足した**（#2381）。19件すべてが誤検知。
    13件は `〜てて` の縮約（`待ってて` `見てて` `書いてて`）と `体系だてて` で、
    残りは強調・反復（`大大大募集` `わかるわかる` `とかとか` `だだ被り` `渡す渡す詐欺`）。
    単語単位でしか許可できないため `て` `とか` `だ` `大` `わかる` `渡す` を挙げている
  - **`sentence-length` は日本語以外が40文字以上続く塊を数えない**（#2381）。一文200文字の上限は
    **日本語の読みやすさ**の指標で、URL・識別子・英文の引用・クエリ文字列は読み下す対象ではない。
    `skipPatterns` に `/[^ぁ-んァ-ヶ一-龥]{40,}/` を置いて、83件のうち33件を外した
    （英文の引用・参考文献の書誌・Timelion のクエリ・環境変数の羅列・LaTeX の数式）。
    40文字は URL やクエリが該当し、文中の英単語（10文字前後）は残る境目
    - **段落内の改行をまたいで数えられていたものは記法を直した**（17件）。`breaks: true` なので
      段落内の改行は `<br>` になり読者は行ごとに読むが、textlint は `。` が無い限り繋げて1文と見る。
      列挙は箇条書き、書籍・発言の引用は `>` の引用ブロック、発言の並びは段落に分ける
    - **番号付きリストの入れ子が壊れていた箇所がこれで見つかった。** `1.` の下に4字下げで `2.` と
      書いても**入れ子にならない**（CommonMark では `1.` 以外の番号は段落を中断できない）。
      親項目の地の文として連結される。入れ子は `*` にするか、番号を `1.` から始める
- **`.ejs` も textlint の対象**（`.textlint/textlint-plugin-ejs`、#2652）。外部リンクの明示チェックのため。
  - プラグインは **`<a href>` だけを Link ノードにして渡す。** 地の文・EJS コメント・属性値は
    AST に出さない。テンプレート中の日本語は UI ラベルと実装コメントで、記事の文章向けの
    ルール（文長・助詞の連続・句点）を当てても意味が無い
  - **加工した文字列を `{ text, ast }` で返してはいけない。** `--fix` がその加工後の文字列を
    「直した結果」としてファイルに書き戻し、**EJS が空白の塊になる**（実際に壊して気づいた）。
    原文をそのまま渡す
  - そのため Document の原文を直接舐めるルール（`no-img-without-dimensions` /
    `no-img-without-lazy` / `no-img-ratio-mismatch` / `no-glued-html-attributes`）は
    **`.md` 以外では降りる**。ガードが無いと `<%= image_size_attribute(…) %>` や
    JavaScript 中の `/<img [^>]*src=…/` を画像タグとして拾う
- `.markdownlint-cli2.jsonc`: 行長・生URL・インラインHTMLなどは無効化済み
- PR には reviewdog が textlint を回し、変更行にレビューコメントを付ける（`.github/workflows/reviewdog.yml`）

## ブランチ / デプロイ

- 作業は `feature` ブランチで行い、`main` へ PR を出してマージする
- `main` への push で `.github/workflows/deploy.yml` が hexo generate → GitHub Pages へデプロイ

## 手で編集しないファイル

以下は GitHub Actions（`update-cache.yml`、毎日 9:00 JST）が生成・コミットする:

- `sns_count_cache.json` / `ga_cache.json` / `ga4_pv.json` — SNSシェア数・GA の PV キャッシュ
- `temp.json` — `snssharecount` の出力用一時ファイル
- `db.json`（112MB、gitignore 済み）— Hexo のビルドキャッシュ

**`scripts/` のフィルタを足したり直したら `make clean` してからビルドする。**
Hexo は記事の描画結果を `db.json` に持っていて、記事の内容が変わらなければ再描画しない。
フィルタだけ変えても既存記事には効かず、最近更新した記事にだけ効いた状態になって原因を見誤る（#2517）。

`normalize.mjs` は日本語ファイル名・本文の Unicode NFC 正規化を行うスクリプトで、
週次ワークフロー（`normalize.yml`）から実行され PR を作る。手動実行は `node normalize.mjs`。

## CSS / 文字サイズの方針

CSS は `themes/future/css-src/` にあり、`scripts/combine_css.js` が
bootstrap-subset → theme-styles.styl の順で `/css/site.css` に連結する。
**この順序は後勝ちの前提なので変えると表示が壊れる。**

文字サイズは過去に「どこで最終値が決まるか追えない」ことが原因の不具合を
2回出している（#1927 / #1928）。以下を守る。

- **1つの要素のサイズは1箇所でしか決めない。** 幅によって変える場合は
  メディアクエリを重ねず `clamp(下限, 可変, 上限)` で1行にまとめる。
  記述順に依存しなくなり、幅の変化に対しても連続する
- **`clamp` の中で演算するときは `unquote()` で囲む。**
  Stylus は `1rem + 0.4vw` の単位を同一視して `1.4rem` に計算してしまい、
  「可変にしたつもりが固定」になる。**描画結果を見ないと気づけない**ため、
  この罠は #1983 と #2008 で2回踏んでいる

  ```styl
  // NG: clamp(17px, 1.4rem, 20px) に潰れて常に 20px になる
  font-size: clamp(17px, 1rem + 0.4vw, 20px)
  // OK
  font-size: unquote('clamp(17px, 1rem + 0.4vw, 20px)')
  ```

- **ブラウザ既定のサイズに頼らない。** 指定が無いと UA スタイルシートの値が
  効き、CSS を読んでも実効サイズが分からない。`h1.list-page` がこの状態だった（#2004）
- 実効サイズの基準:

  | 対象 | 値 | 指定箇所 |
  | --- | --- | --- |
  | `body` | 13px | `theme-styles.styl` |
  | 本文（`p` / `li` / `summary`） | `1.2em` = 15.6px | 〃 |
  | 記事タイトル | `clamp(26px, 1.325rem + 0.9vw, 32px)` ＋ `text-wrap: pretty`（#2655。均等化すると「列幅の半分×2行」に縮む） | 〃（1箇所のみ） |
  | クラス無しの `h2`（関連記事・We're hiring 等） | `text-display`（#2971。`1.85em` = 24.05px から） | 〃 |
  | サイドバーの `h2` | `text-lead`（#2971。`1.4em` = 18.2px から） | 〃 |
  | 本文見出し h2〜h6 | `1.85 / 1.5 / 1.3 / 1.2 / 1.1em` = 24.05 / 19.5 / 16.9 / 15.6 / 14.3px（太さは全て700。直前の空きは h2 56px / h3 32px / h4 以下 24px）。`text-wrap: balance` は**幅768px以上だけ**（#2776 / #2822。狭い列では2行になる見出しが h2 の19.8%に達し、均等に割ると1行目が列の65%しか埋めず折り返しが早く見える） | 〃 |
  | 本文見出し h1（`.article-entry h1`） | 記事タイトルと同じ（`.article-title` / `.list-page` と同一ルール。直前の空き 64px・罫線 2px はここで持つ） | 〃 |
  | 一覧ページの見出し `.list-page` | 記事タイトルと同じ（`.article-title` と同一ルール） | 〃 |
  | 一覧ページの統計（数値 / ラベル） | `text-body` / `text-meta`（#2911 で 20px から下げた。サイドバーの内寸 208〜298px では 20px が行の6割を占めた。統計は一覧ページの主役ではないので #1927 の 25px → 20px と同じ向きにもう一段。#2971 で 16px → 段の 15.6px へ） | 〃 |
  | 全件への導線（`.more-link`） | `text-row`（#2893。押す対象なので「行として並ぶもの」の段）。**カードの中では、そのカードの地の文と同じ段**（特集パネルは `text-meta` #3014、著者紹介は `text-base` #3019。いちばん弱い情報が説明文より大きくなっていた。箱ごとに地の文の大きさが違うので値も違う） | 〃 |
  | 目次の項目（`.toc-section ol`。サイドバー・モバイル共通） | 14px（字下げの `1em` もこの値に追従する） | 〃 |
  | 目次のラベル「目次」（`.toc-section h2` / `.toc-mobile summary`） | 14px（サイドバーの `h2` の例外。見出しではなくブロックのラベル） | 〃 |
  | 脚注（`#footnotelist li`） | `1em` = 13px | 〃 |
  | 記事概要文 `.lede`（一覧 / We're hiring カード） | `1.2em` = 15.6px / 継承 = 13px | 〃 |
  | 特設ページの本文 `.specials-text` | `1.2em` = 15.6px（`.article-entry` 外なので明示） | 〃 |
  | コードブロック | 13px（`line-height` は `font-size × leading-row` で追従） | `highlight.styl` の変数 |
  | ヘッダーのサイト名 `.header-title` / 和文 `.header-title-sub` | `text-lead` / `text-meta`（#2877 で写真の上の 80px / 20px から。#2971 で 17px → 段の 18px へ。字間 0.35em と影は白地では役が無い） | 〃 |
  | ヘッダーのナビ `.header-nav-link` / ドロップダウン（見出し / 説明） | 14px / 13px / 12px | 〃 |
  | パンくず（`.breadcrumb`） | 14px | `theme-styles.styl` 冒頭（土台の塊）の `font:` 一括指定。フォント名だけ後方で上書き |

- コードブロックのサイズは本文との相対バランスを見て 15px → 14px → 13px と
  調整した経緯がある（#1927 / #1929）。安易に変えない
- **文字サイズの段は7つ**（#2971）。`_variables.styl` が持ち、**役で選ぶ**。
  上の表は段そのものではなく、**どの部品がどの段に乗っているかの台帳**

  | 段 | 値 | 使うところ |
  | --- | --- | --- |
  | `text-meta` | 12px | 添え・注記・件数・小ラベル |
  | `text-base` | 13px | 土台（`body`・コードブロック） |
  | `text-row` | 14px | 行として並ぶもの（ナビ・目次・パンくず・全件導線） |
  | `text-body` | 15.6px | 読み下す文章（記事本文・特設ページの本文・概要文） |
  | `text-lead` | 18px | サイト名・サイドバーの見出し |
  | `text-title` | 20px | 見出し・ロゴ・一覧カードの記事名 |
  | `text-display` | 24px | 大見出し（クラス無しの `h2`） |

  - **部品は px、本文見出し h2〜h6 だけ em の梯子**（`1.85 / 1.5 / 1.3 / 1.2 / 1.1em`）。
    あちらは比が階層を作る列で、土台の大きさに追従する必要がある（#2524 / #2599。
    行間で `leading-heading` を独立させたのと同じ扱い）
  - **段の外にあるのは4つだけ。** 記事タイトルの `clamp`（幅で変える唯一の値）・
    404 の 128px・タグの件数の 6.5px・**行き先の名前に付く総数の 8px**
  - **`font-size` でアイコンの実寸を決めている箇所は段の外**（`.svg-icon` が `1em` なので、
    親の `font-size` がそのまま絵の大きさになる）。文字ではないので役の名前が付かない。
    `.snscount-icon` の `1.15em`（♡ の線が潰れないように記号だけ一段大きくする）も同じ
  - **機能で決まっている値も段の外。** `.header-search input` の **16px** は
    「これを下回ると iOS がフォーカス時に画面をズームする」ための下限で、
    文字の大きさとして選んだ値ではない
  - **`bootstrap-subset.css` の値は対象外**（`code` `kbd` `pre` `small` `sub` `sup` の
    `.875em` / `.75em`）。vendor の reset で、`combine_css.js` が先頭に連結する
  - **添えの小さい数字・注記は `ink-mute` × `text-meta`。** 以前は `0.85em`（11.05px）・
    `0.9em`（11.7px）・`10px`・`11px` に分かれていたが、#2971 で1つに寄せた
    - 例外は**行き先の名前に付く総数**（`.header-dropdown-count` / `.sidebar-count`）で、
      こちらは `ink-faint` × **8px**（段の外）。行の主題は名前で、数字はそれを選ぶ
      判断には効かない。段のいちばん下（`text-meta` 12px）でも 13px の名前と
      1px しか違わず、名前の一部に見える。同じ役のタグのチップの件数
      （`0.5em` = 6.5px）が既に段の外にあるので、そちらに寄せた
  - **`em` は親に掛かる。** `1em` は「サイズを決める」のではなく
    **親の倍率を打ち消す**指定（`.article-entry details p` の親は `1.2em`）。
    `.summary-count` の「em の連鎖をやめて px で書く」（#1927 / #1928）は
    **2段の掛け算**（`1.2em × 1.6em = 25px`）の話で、`body` に1回掛けるだけには当たらない
  - **見分けの付かない差に2つの役を割り当てない。** 集めた時点で site.css の
    `font-size` は132件・うち123件がサイズを決めていて、相異なる値が26種類あった。
    隣り合う差が 1px 未満の組が19組（`.footer-count` 11.00px と
    `.archive-list-count` 11.05px は 0.05px 差）。`ink` を3段に絞った #2534 と同じ理屈
  - **新しい部品は段のトークンで書く。px や em を直接書かない。** #2971 と並行して入った
    #2908 が `1.4em` と `12px` を直接書いていて、**寄せ終えた直後にまた段の外へ出た**
    （「段が無いので実在する値から選ぶ」は #2975 より前の書き方）。
    **再発は `font_size_lint.mjs` が止める**（`make lint-css`、PR では `css` ワークフロー）。
    段の外に置くものは**このスクリプトの `EXEMPT` に理由付きで1件ずつ挙げる**。
    「なんとなく実在する値を選んだ」を通さないための一覧なので、増やすときは理由を書く
- **行間の段は3つだけ**（#2864）。`_variables.styl` が持つ。サイズと同じく
  「1つの役に1つの値」で、部品ごとに決めない

  | 段 | 値 | 使うところ |
  | --- | --- | --- |
  | `leading-heading` | 1.3 | 記事タイトル・ページ見出し・本文 h1〜h6・サイト名・カードとパネルの見出し |
  | `leading-body` | 1.75 | 読み下す文章。記事本文・特設ページの本文・note の中・フッターの説明 |
  | `leading-row` | 1.4 | 行として並ぶもの。一覧・目次・パンくず・ページャ・チップ・タブ・カードの説明・コードブロックの1行。`body` の既定でもあるので、`line-height` を書いていない要素はここを継ぐ（#2868） |

  - 段を2つに分けるのは、**和文の長文と一覧の行では行間の目的が違う**ため。
    和文は仮想ボディいっぱいに字が入るので段落は 1.75 まで開けないと目が次の行の頭へ
    戻れない（Zenn 1.8 / Qiita 1.7 と同じ帯）。一方、一覧・目次・チップは読み下す
    対象ではなく、開けるほど帯が縦に伸びて一度に見える件数が減る
  - **`line-height` を書かないままにしない。** 以前は本文 h3・h5・h6 とパンくずが
    指定を持たず、フォント依存の値で描かれていた。原因は `font:` の**一括指定**で、
    これは `line-height` を `normal` に戻す（`body` と `.breadcrumb` がこの形）。
    `h1, h2, … h6 { font: 300 'Open Sans', sans-serif }` の方は `font-size` を
    省いていたため宣言ごと捨てられており、そもそも何も効いていなかった
  - **1行しか入らない箱の `line-height: 1` は段の外。** 順位の丸・アイコン・
    シェア数のように、行間ではなく箱の高さを決めている値
  - 集約前は見出しが4通り（1.4 / 1.225 / `normal` / `!important` 付きの 1.5）、
    文章が7通り（1.75 / 1.8 / 1.9 / 1.6 / 1.5 / 1.4 / 1.42857143）に散っていた
- **間隔（`gap`）は4の倍数だけを使う**（#2864）。刻みは `space-step`（4px）が持ち、
  段の数は決めない（並びの間隔だけで 4〜48px の幅がある）。
  4px 刻みなら隣の段が必ず 4px 差になり、**5px と 6px のように見分けの付かない差**が
  生まれない。以前は 2 / 3 / 5 / 6 / 10 / 14 / 25px が混ざっており、多くは
  「円の直径の半分」のようにその場で計算した値で、隣の部品と揃える理由が無いまま
  1px ずつ違っていた
- **サムネイルの寸法は4つしかない。新しい値を作らず、器の形で選ぶ**（#2790）。
  いずれも `object-fit: cover` で切り抜き、角丸は `radius-small`。サムネの実寸は
  幅300px・高さがばらばら（比 1.0〜3.2）なので、寸法はこちら側の枠が決める

  | 器 | 寸法 | 使うところ |
  | --- | --- | --- |
  | 行に添える（`post-list-icon`） | 72×48 | 関連記事・被参照・ランキング・索引記事の表 |
  | 横並びのカード（`panel-thumb`） | 88×59 | サイドバーの枠・記事末のパネル・We're hiring |
  | パネルのグリッド | 200×135 | ホームの連載/特設パネル・`/series/` 一覧 |
  | 一覧カードの頭（`card-thumb`） | 幅100%・`aspect-ratio: 1200/630` | 記事一覧のカード（OGP と同じ比） |

  - 比は 3:2 前後に揃っているが完全には一致しない（1.500 / 1.492 / 1.481）。
    200×135 が標準サムネの寸法で、88×59 はその比を88px幅に丸めた値。
    72×48 だけ 3:2 ちょうどなのは、48×32 では何の画像か読み取れず
    比を保ったまま1.5倍にした経緯（#2777）。切り抜きで吸収されるので揃え直さない
  - **72×48 より小さくしない**（#2777）
- **状態は新しい色を足さずに表す**（#2864）。動かすのは太さか面のどちらか一方で、
  両方は動かさない（#2722 で踏んだ）
  - **現在地（active / current）の表し方は、その部品が押す面を持つかで決まる。**
    文字だけが並ぶ帯（パンくず・年月一覧・サイドバーのカテゴリ）は**太字**、
    丸や箱の的を持つ部品（ページャ・タブ）は**面**を塗る。帯の中で面を塗ると
    現在地だけが浮き、的を持つ部品で太字にすると押せる範囲が変わらず所在が読めない
  - 現在地はリンクにせず `aria-current="page"` を付ける（#2705 / #2722）
  - **無効・空は `ink-faint`（文字）と `surface-mute`（面）**。枠と大きさは中身が
    あるときと同じに保ち、場所だけ空けておく（投稿が無い月・サムネの無い連載）。
    hover には応えない。**`ink-faint` より薄くしない**（白地で 4.5 を満たす最も薄い段）。
    無効な項目も何であるかは読める必要がある
  - 押す対象ではない添え（シェア数）は押せる部品の**外**に出す。押せる範囲と
    数字が別であることを位置で示す
- **チップの左端は、隣が「文字」か「行」かで揃える相手が変わる。**
  チップは枠の内側に文字があるぶん、素で置くと文字が枠1px＋左パディング8pxぶん右へずれる
  - **同じ行に文字が並ぶなら、負のマージンで戻して文字どうしを揃える**（#2429 / #2453）。
    `ul.tag-list` の `-13px`、`.awards-hall-years` の `-8px` がこれ
  - **チップが自分の行・自分の箱を持つなら、枠の側を揃える。**
    `.header-search-hint ul.tag-list`（囲みの中）と `.article-header-tags`
    （#2907 でメタ行から自分の行へ移った）がこれ
    - **ただしピルは箱の左端では揃わない。** 最も左に張り出すのは**縦の中央の1点だけ**なので、
      箱の左端を 0 に置くと図形が右へ寄って見え、9px 戻すと枠が食い出して見える。
      **実効半径（高さの半分）のさらに半分だけ戻す**のが落とし所
      （`.article-header-tags` は高さ 21.8px・半径 10.9px なので `-5.4px`）
    - この値は**4の倍数の刻み（#2864）の外**。間隔ではなく光学補正だから
  - **同じ部品が移動したら、この判断はやり直す。** `.article-header-tags` は
    メタ行に居た頃の `-9px` を持ったまま行に移っていた
- **見出しの階層はサイズ・太さ・直前の空き・罫線で作る**（#2524 / #2599）。
  サイズだけで表そうとすると上下が詰まる。実際、記事タイトルの下限が 24px
  だった頃は 320〜414px の幅で本文 h2（24.05px）と同じ大きさになっていた
  - 避けたいのは**同着**で、比を大きく取ること自体が目的ではない。下限を 28px
    （比 1.16）まで上げるとモバイルで字数が足りず、幅375pxで4行以上の記事が
    12% 出たため 26px（比 1.08）に戻した。太さ（800 対 700）・ページ先頭という
    位置・h2 側の空き 56px と罫線が階層を支えている（#2599）
  - **モバイルの字数を数える。** 幅375pxの本文列は351pxで、全角で入る字数は
    26px なら13.5字・28px なら12.5字。1px の差が1行あたり1字を動かす
  - **h2 の罫線が薄い（`rule-weak`、白地とのコントラスト比 1.19）のは意図。**
    節の区切りは直前の空き（56px）が担っていて、線は「境目がある」と
    気づかせる程度でよい。読みやすさの指標ではないので濃くしない
  - タイトルは長く（中央値29文字）本文 h2 は短い（中央値7文字）。
    同じ 1px でも折り返しのコストが両者で違うので、サイズを動かすときは
    どちらの話かを分けて考える
  - **本文の h1 はページタイトルと同じ大きさにする**（#2577）。Markdown で書いた
    特設ページ（`/specials/markdown/` 等）はレイアウトが `page.title` を出さないため、
    本文の h1 がそのページの唯一のタイトルになる。EJS で書いた特設ページの
    タイトルは `.list-page` なので、揃えないと同じ役割の見出しが 26px と 32px に割れる
  - **記事本文で h1 を使わない。** 記事タイトルが h1 相当なので `##` から始める。
    h1 を節見出しに使うと記事タイトルと同じ大きさで出る（#2526 も参照）
- 色を変えるときは WCAG AA（コントラスト比 4.5）を満たすか計算してから入れる
  - **計算結果はスタイルガイド（`/specials/styleguide/`）が出す**（#2856）。トークンの
    見本・16進数・コントラスト比はすべて `_variables.styl` の変数から生成しているので、
    変数を変えればページの表示もそのまま変わる。**この表に数字を書き写さない**
  - **ダークの段は別の節に出す**（#3012。24トークン）。明るい側の表に列を足さない。
    **値が別物なので同じ表に混ぜても比較にならず**、列を倍にすると狭い幅で表が読めなくなる。
    比の地はページの背景（`dark-surface-base`）で、リンクだけ地が3種類あるので分けて測る
  - **散文にも測った数字を書かない。** 表と違う測り方の値（「いちばん厳しい地で 4.74」）や、
    表から書き写した値は、変数を変えたときに嘘になる。**閾値の名前（AA）と、
    表のどこを見比べればよいかを書く**
  - 文字の大きさの見本も同じ考え方で、コア側のセレクタに `.sg-title` / `.sg-h2`〜`.sg-h6`
    を足して実物の値を借りている。見本のために `font-size` を書くと、サイズを決める
    場所が2つに増える（#1927 / #1928 の再来）
  - 行間と間隔の見本も同じ（#2864）。行間は `leading-*` を、間隔は `space-step` に
    倍数を掛けた値を `content` に出しているので、ページに px が1つも書かれていない
  - **`ink` / `rule` / `surface` の段の外に色を置かない**（#2859）。寄せ先は
    **近い段との差**で決める。#2534 で「`#616161` と `#6e6e6e` は明度差 13/255 では
    判別できない」として段を3つに絞ったので、**差 13 前後なら同じ色**と見なして寄せる。
    差が見分けられないのに別の役として置かれていると、どちらを使うかの判断ができなくなる
    - 寄せた7箇所（#2859）: 記事のメタ行 `#555`・カテゴリの説明とページング情報
      `#5f6d7b`（青みのあるグレー。`ink` の3段はすべて無彩色なので色相を増やさない）→
      `ink-mute`、畳みの三角 `#767676`（差1）→ `ink-faint`、ツールチップの点線 `#bbb` →
      `rule-strong`（線なので `rule` の段）、RSS アイコンの hover の純黒 → 下記
    - **役があって残す色は2種類だけ**（計13箇所）。note の4色と表彰・殿堂の淡金9箇所。
      リンクの状態色5箇所は #2860 でトークンにした
  - **地の文のリンクは色だけで名乗らない。下線を持つ**（#2926）。`link-blue` と地の文
    （`ink-strong`）の比は 2.17 しかなく、WCAG 1.4.1（レベル A）が求める 3:1 に届かない。
    **色では直せない**——白地で AA（4.5）を保つ輝度（≤ 0.1833）と、地の文と 3:1 を作る
    輝度（≥ 0.2634）は重ならず、暗い側は負になる。地の文を #2C2C2C 以下まで暗くすれば
    未訪問だけは通るが、訪問済みの紫は**地の文を純黒にしても成立しない**（未訪問と
    同じ輝度の帯に入り、色相でしか見分けられなくなる）。`:visited` に下線を足す手も
    使えない（履歴の覗き見対策で、書けるのは色系のプロパティだけ）
    - **当てるのは地の文の器**（`.article-entry` の `p` / `li` / `td` と
      `p.specials-text`）。`.article-entry a` でまとめると見出しのアンカー（113個）と
      We're hiring のカードまで拾う
    - 下線は `currentColor` なので**訪問済みは紫の線になる**。`text-decoration-color` を
      書く必要はない。`text-underline-offset: 3px` はサイトに既にある唯一の値
      （`.blog-info-metric span[title]`）と同じ
    - **静止で下線を持つぶん、反応は線の太さで返す**（1px → 2px）。反応が起きる場所は
      下線のままなので hover の型は増えていない
    - 脚注（`sup` の番号と `#footnotelist` の戻り）は上付きの数字と記号で地の文に
      紛れないので下線を持たない
  - **リンクの色は4値・3状態**（#2860）。`_variables.styl` が持つ（`link-blue` /
    `link-visited` / `link-on-tint` / `link-on-note` / `link-on-note-visited`）。
    地が濃くなるぶん暗い側へ振る段で、`link-blue` は白地では AA を満たすが
    インラインコードの地（`surface-mute`）と note の4色の上では下回る。
    訪問済みの紫は白地とコードの地の両方で足りるので1値でよい
    - **比を使う場所に書かない。** スタイルガイドが地ごとに計算で出す（note は
      4色のうちいちばん厳しい地との比を載せる。AA を満たすかはそこで決まる）。
      以前このコメントに書いてあった 3つの数字は**どれも実測と違っていた**
      （AA を割るのは `link-on-tint` ではなく `link-blue`、`4.13` は再現せず、
      「4色すべてで 6.0 以上」は実際には 7.31〜8.84）
  - **青は記事本文の参照リンクの色。レイアウトの部品には使わない**（#2893）。
    全件への導線（ホームの連載・特設の枠、サイドバーの人気のタグ、記事末の連載ナビと
    著者紹介の `.more-link`、ヘッダーのドロップダウンの `.header-dropdown-more`）は
    `ink-mute`。hover は他の添えのリンクと同じ `ink-strong` ＋下線（#2699）
    - **青をやめたら手がかりを別に足す。** 14px の1行では色しか手がかりが無くなるので、
      末尾に `>`（`chevron-right`）を置く。ドロップダウンの側は面 hover と
      仕切りの線を持つ行なので記号は要らない
      - **記号を使うのはこの導線だけ。** `chevron-right` はサイト全体で
        `.more-link` の6箇所にしか出ない。装飾として他の場所には置かない。
        規則はスタイルガイドが名乗る（#3014）
      - **左端に揃える。詳細度を合わせるため `p.more-link` と書く**（#3017）。
        単一クラスでは `.blog-posts p` の `justify`（0,1,1）に負ける。
        以前は `.more-link` に `text-align: right` があったが、本文の中では
        justify に負け、サイドバー2つと著者紹介は `left` で打ち消していて、
        **右へ寄る場所は1つも無かった**（5文脈・2幅で実測）。宣言を4つ落として
        実態の1つにした
    - **文言は「◯◯一覧へ」**（#2899）。動詞を落として行き先の名前だけにする。
      `/series/`「連載一覧」・`/tags/`「タグ一覧」のように**行き先の h1 と同じ語**を使い、
      末尾の `へ` と `>` が「そこへ行く」を担う。以前の「すべての◯◯を見る」は
      14px では 143.5px あり、内寸142pxのサイドバーで2行に折り返していた
      （「連載一覧へ」は 87.5px）。**「すべての」は要らない。** 全件でないなら
      枠の見出しが「人気の」と名乗っている (#2670) ので、導線の側で断る必要がない
      - 例外は著者紹介の「執筆記事一覧へ（N本）」だけ。行き先の h1 は
        「◯◯さんのページ」だが、著者名は枠の見出しが既に名乗っているので繰り返さない
    - **「次の行き先」は全件でなくても `.more-link`**（#2900）。カテゴリページの
      「◯◯カテゴリの◯◯以外の記事へ（N本）」は絞り込んだ一覧への導線だが、
      同じ部品にする。**同じ言い回しのリンクが2通りの見た目を持たない**ようにする方を優先する
      - **置くのは h1 の説明文の直下。** これは「次に行くところ」ではなく、
        **いま見ている一覧の偏りについての断り**（1つのタグがカテゴリの過半数を
        占めている #2038）なので、一覧を読み始める**前**に要る。以前は関連カテゴリの
        直下にあったが、あそこは読み終えた読者への次の行き先で役が違う
      - **部品は `.more-link` のまま、位置だけが違う。** 見た目を変えるのではなく
        置き場所で役を示す。出るのは1ページ目だけ（関連カテゴリは最終ページにも出る）
      - 対になる絞り込み側のページの注記（`.list-page-note`）は寄せない。
        あちらは絞り込みの状態の説明で、リンクを持たない文も含む地の文
    - **太字にはしない。** `.publish-date` / `.post-author` は 700 だが、
      14px の1行を太らせると行の中で主従が付く前に重くなる（#2803）。
      太さの段は 400 / 700 / 800 の3つで、中間は持たない
    - **その節の主目的は全件導線ではない**（#2893）。ガイドラインの
      「◯◯ を読む」は3つの実体サイトへの入口＝そのページの本文なので、
      説明文と同じ `.specials-text`（15.6px・左揃え・`link-blue`）にする。
      右寄せ 12px の添えに入れると、節でいちばん弱い表現になる
  - **下線と面は同時に動かさない**（#2893）。面で反応する部品（行・チップ・
    アイコンだけのリンク）は `text-decoration: none` で下線を打ち消す。下線が付くのは
    面を持たない部品で、`.breadcrumb a` / `.publish-date` / `.post-author` /
    `.article-category-link` / `.author-list-link` / `.category-index-name` /
    `.series-nav-title > a` / `.series-nav-succession` / `.tag-cloud a` /
    `.more-link a` と本文、そして**カードのタイトル**がこの形。
    **スタイルガイドがこの規則を名乗るので、例外を1つ作ると嘘になる。**
    `.tag-cloud a` は色だけ動かしていたので #2893 で揃えた
    - **カードは下線側。** #2747 で囲みも影も失って面を持たなくなった。以前は
      中身の色を一段落として返していたが、`ink-strong` → `ink-mute` の**状態の差は
      1.62:1** しかなく読み取れない（状態は前後を並べて比べられないので、同時に
      見える2色の判別より大きい差が要る）。色を濃くする方向は `ink-strong` が
      最濃で行き止まり、青は地の文の参照リンクの色なので使えない。
      下線は #2948 で地の文が常時持つようになり語彙に入っている。
      画像側の `opacity: 0.7` はそのまま（文字と画像で別の反応を持つ #2449 の形）
    - 打ち消しの2箇所（`.panel-title:hover` / `.career-counseling .article-card a:hover`）は
      metronic の `a:hover` の青を止めるために残すが、色は `ink-strong` にする。
      `ink-mute` のままだと、タイトルやラベルを直接指したときだけ余分に変わる
    - 例外は2つだけ。**ヘッダーの帯**は下線も面も動かさず文字色を一段濃くする
      （ドロップダウンの行は面）。**フッター**は `::after` の下線が左から伸びる
  - **アイコンだけのリンクの hover はチップの型**（地が一段変わる、#2859）。
    絵柄には**下線が引く相手が無い**ので、丸い地を持たせてそこを一段変える。
    フッターの丸（`.footer-icon`）と記事のシェアボタン（`.share-btn`）が既にこの形で、
    カテゴリページの RSS アイコンだけが面を持たず純黒に変わっていた。
    `.share-btn` を付け直して形ごと寄せた（5つ目の hover の型は作らない）
  - **1つの部品が面と文字色の両方を動かさない**（#2859）。`summary:hover` が
    要素セレクタで文字色を変えていて、同じ summary に当たっている面の反応
    （`surface-mute`）と二重になっていた。宣言ごと落として面だけにした
  - **スタイルガイドと記法ガイドに開発者向けの誘導を書かない**（#2864）。CLAUDE.md や
    リポジトリのファイルパスへのリンクは読者にとって行き止まりになる。
    トークン名・クラス名は決まりの語彙なのでそのまま出してよい
  - **スタイルガイドに「ページの作り方」と「整合の担保」を書かない**（#2895）。
    値をどこから引いているか・両方に書く必要があるか・片方だけ直すと嘘になるか、は
    リポジトリの中の話で、**読者が語られたいのはスタイルそのものだけ**。
    落とすのは3種類。**ページ自身の作り**（「このページの数字は手で書いていません」
    「下の見本は実際のルールをそのまま借りて描いています」）、**値の置き場所**
    （「`theme-styles.styl` と `highlight.styl` の両方に書く必要があります」
    「1つの要素のサイズは1箇所でしか決めません」「行間を書かないままにしません」）、
    **ソースの出現回数**（「残っている色は13箇所」「淡金9箇所」「呼び出し元 19」）
    - **見た目の決まりは残す。** 「段階は3つだけ」「間隔は4の倍数だけ」「新しい色を
      入れるときは近い段階との差を見て寄せられないかを確かめる」は読者が確かめられる
      スタイルの話。落とすのは**どこに何回書くか**の側だけ
    - 部品の一覧（`_partial/*` のパスと呼び出し元の件数）は節ごと落とした。
      `## 部品` に残るのは note / 表 / チップの見本
    - **記法ガイドの「公開時にこちらで整えます」は残す。** あちらの読者は寄稿者で、
      「自分がやらなくてよい範囲」は必要な情報。落としたのは運営が測った数字
      （「連番のある記事80本のうち6本で番号が飛ぶ」）とビルドの仕組み
      （「ビルド時に SVG へ変換され」）の2つだけ
  - **スタイルガイドに過去の経緯を書かない**（#2873）。「以前は〜だった」は git と issue が
    持っている。残すのは**これから判断するときの物差し**の方で、散らかっていた値の列挙は落とす
    （「見分けられない差に2つの役を割り当てない」「明度差 13/255 なら同じ色と見なす」は残し、
    `#616161` と `#6e6e6e` が並んでいた話は消す）
  - **本文はフロントエンド一般の語で書く**（#2873）。リポジトリ内の語彙は読者に通じないので、
    ページでは段階 / スケール・背景・フォーカスリング・補助情報・ヒットエリアと書く
    （このファイルの `段`・`地`・`輪郭`・`添え`・`的` に対応する）。
    **トークン名と1対1の語はそのまま**（面＝`surface`・罫線＝`rule`・文字＝`ink`）。
    CLAUDE.md 側は内部の語彙のままでよく、揃えるのはページの本文だけ
- **ダークモードは `:root` のトークンを差し替えて作る**（#2746）。状態は3つ——
  何も指定しない（OS の設定に従う）/ `data-theme="dark"` / `data-theme="light"`。
  **2値のトグルにするとシステム追従へ戻せなくなる**
  - **属性は `head` の中の同期スクリプトが `localStorage` を読んで付ける。**
    外部ファイルにすると読み込み待ちの間に明るい画面が一瞬出る（FOUC）
  - **トグルの絵と読み上げ名の出し分けは CSS が `:root[data-theme]` を見て行う。**
    3つの状態ぶんを DOM に置いて1つだけ出すので、描画時に JS を待たない。
    「端末の設定に従う」はディスプレイの絵（`device-desktop`）が通り相場で、
    太陽と月を合成した絵では「自動」が読み取れない
  - **同じコントラスト比でも、明るい面は暗い面より膨張して見える。**
    白地の濃い丸をそのまま反転すると暗地では浮く（著者の頭文字の丸で踏んだ）。
    **同じ強さを移すのではなく、その部品が担う役から決める**。所在・反応の印は
    明るいまま、装飾は一段落とす
  - **ブランドのネイビーは暗い地で 1.05 しか出ない。** 面としても線としても消えるので、
    フッターの地は一段暗い値にして色味だけ残し、差し色として使っていた箇所
    （ページャの現在地・タブの上辺・共有ボタン・タグの hover・フォーカスの輪郭）は
    明るい線・明るい塗りに置き換える
  - **地に色が付いた面は、中の文字と一緒に暗くする。** note の4色と表彰の淡金カードは
    地だけ明るいまま残すと、中の文字がテーマ側に付いていって沈む。
    ページとの差を 1.25〜1.40 にそろえて帯として読ませる
  - **「地より一段濃い」は暗地では「一段明るい」。** note の中のインラインコードと
    表ヘッダは `code-bg()` で計算していたが、持ち上げる向きが逆になるので
    暗い側は値を直接持つ
  - **`bootstrap-subset.css` が持つ値はトークンで上書きする。** `.card` の地
    （`#fff`）と枠（`rgba(0,0,0,.125)`）を連載ナビのパネルと著者紹介が継いでいた
  - **echarts の option では CSS 変数を参照できない。** `:root` から実効値を読んで
    option に流し込む（`_partial/chart-theme.ejs` が1箇所で持ち、各チャートは
    `themeChart(インスタンス)` を1回呼ぶ）。切り替えは `themechange` イベントで知らせる
- **暗い地の上には別の段を使う**（#2839）。`ink` / `rule` / `surface` は白地の上で
  決めた値で、ネイビー地では `ink-strong` が 1.63 しか出ず1つも使えない。
  暗地用の段は `_variables.styl` が持つ（`on-dark-strong` / `on-dark` / `on-dark-mute` /
  `navy-raised` / `rule-on-dark` / `crimson-on-dark`。CSS からは `--on-dark-strong`
  のように CSS 変数で引く）。いずれもブランド色の
  明度違いか無彩色で、**新しい色相は足していない**。ダークモード（#2746）でも同じ役
  - **暗地では「面」と「線」を同じ値にしない。** 明度だけ上げると彩度が残るので
    （地も `--navy-raised` も彩度 81%）、1px にすると鮮やかな青の筋に見える。
    線は白を薄く重ねた `--rule-on-dark` を使う。明るくなると同時に彩度が落ちて
    （50%）色味が引く。白地では `rule-*` と `surface-*` が既にこの役を分けている
  - **差し色は役で分ける。** `--brand-crimson` は暗地で 3.06 しかなく線や記号として
    見えないので、そこには一段明るい `--crimson-on-dark`（3.62）を使う。
    逆に塗り面は `--brand-crimson` のまま（白文字 5.35。明るい方は 4.51 で余裕が無い）。
    **暗地で「読める差し色」と「白文字を載せられる差し色」は同時に成立しない**
  - **フォーカスの輪郭も段を替える**（#2847）。所在の目印は `a:focus-visible` の
    ネイビー2px を1箇所で持つが（#2686）、暗い地ではその色が見えない。
    フッター（同じネイビー地）で 1.00 しか出ない。暗地では `--on-dark-strong` に替える
    - 反応（hover）と所在（focus）は別の役なので、**輪郭に差し色を使わない**。
      フッターの hover はクリムゾンの下線で、同じ色にすると2つの状態が混ざる
    - 暗地なのは**フッターだけ**（#2877 でヘッダーが白地の帯になった）。他のネイビー
      （チップ・ページャの現在地・ランキングの順位）は白地の上の小さな塗り面で、
      輪郭は外側の白地に出るため既に見えている
- **ヘッダーは1段・64px の帯**（#2877）。左からロゴ・サイト名（英字＋和文の2段）、
  右にナビと検索を1行で並べる。以前は 1500×360 の写真バナーの上に
  80px の中央タイトルを載せた 151px の塊で、全ページの先頭でそれだけ縦を使っていた。
  高さは `header-height`（64px）が1箇所で持ち、**幅では変えない**
  - **帯の中に縦の罫線を置かない**（#2901）。ナビと検索の間にあった 1px の縦棒は、
    帯の右端から 258px（検索フォーム 234px ＋ 間隔 24px）の固定位置に立つ。
    一方で本文のグリッドの縦線（ランキングのカードの右枠＝本文列 10/12 の右端）は
    帯の右端から `C/6 + 12` px（`C` = `.container` の幅）で、**幅によって動く**。
    比例定数が違うので、**どの幅でも揃わない**（992px以上で 86 / 56 / 26px ずれ、
    一致するのは `C` が 1476px のときで上限 1320px に届かない）。
    26px は「惜しい」距離で、揃え損ねに見える。区切りは 24px の間隔と
    検索フォーム自身の枠線・薄い地が既に付けている
    - **ヘッダーの部品の寸法を本文のグリッドに従属させない。** 検索窓の幅を
      サイドバー列に合わせれば揃うが、サイドバーが縦積みになる幅（1024px以下）で
      その根拠が消える
  - **ヘッダーの帯は追従（sticky）しない。** 64px の帯が全ページで本文の上に
    常に乗ることになり、読んでいる面が狭くなる。写真をやめたぶん
    `backdrop-filter` も要らなくなった
    - **サイドバーの目次は追従する**（#2909。以前は「固定サイドバーを置かないのと
      同じ理由」と書いていたが、そちらはトライアルで入れた）。目次は
      読んでいる記事の中の移動手段で、帯のように全ページに常駐する部品ではない
  - **帯の中身は `.container` の内側に置く。** サイト名の左端がパンくず・本文と揃う。
    下に開くパネル（ドロップダウン・検索のヒント）の位置の基準も `.header-bar` が持ち、
    `top: 100%` が帯の下辺、`right: 0` が本文と同じ右端になる
  - **ドロップダウンは `:hover` と `:focus-within` で開く。** JS は使わない
    （検索窓のヒントパネル #2791 と同じ形）。引き金は `<button>` で、
    ラベル（「リソース」「アクティビティ」）に対応する1枚のページが無いのでリンクにしない。
    押す必要はなく、フォーカスが当たった時点で開く
    - `li` に帯と同じ高さを持たせて、引き金とパネルの間に**隙間を作らない**。
      1px でも空くとそこでカーソルが外れて閉じる
    - **並びは カテゴリ → 連載 → リソース → アクティビティ**（#2906）。記事がメインの
      ブログなので、記事へ届く軸を先に置く。サイドバーの「カテゴリ → 人気のタグ」
      （#2885 / #2880）と同じ順序で、ヘッダーだけ逆にしない
      - **「アクティビティ」は元「コミュニティ」。** 中身（コンテスト・ポッドキャスト・
        アドベントカレンダー）はいずれも自社発の活動の記録で、名前が指すコミュニティ
        貢献（スポンサー・会場提供・勉強会）の実体がまだ無かった。**実態以上のことを
        名乗らない。** その実体ができたら、そのときに節を立て直す。
        「アーカイブ」は今も続く活動を過去形で名乗るので採らない
    - **ドロップダウンは4つで、中身の作りが2種類ある。**
      「リソース」「アクティビティ」は**手で選んだページ3件**＋説明文1行。
      「カテゴリ」は **`/categories/` と同じ全件**（`category_groups()` の群と並び）で、
      18件を5つの群に束ねて2列に並べる。「連載」は
      **`popular_series(6)` の6件**。どちらも説明は持たず名前だけで、
      カテゴリだけ累計本数を添える。名前が主題そのものだからで、
      カテゴリの一言説明は `source/_data/categories.yml` を読む個別ページが持つ
    - **全件でないことはラベルが名乗り、条件は `title` が持つ**（#2670）。
      「人気の連載」がそれ。カテゴリは全件なのでラベルを置かない
    - **2列のパネルの幅は「最長の行が折り返さない」で決める。** `DataEngineering` が
      13pxで101px（Noto Sans CJK 実測）、アイコン 16.9px と3桁の件数 33.7px を
      足して1行151px。列の内寸を174pxまで取れる 420px にしている
    - **2列はグリッドではなく多段組（`columns`）で作る**（#2908）。群を列の途中で
      割らないため（`break-inside: avoid`）。グリッドだと行の高さが左右で揃い、
      件数の少ない群の下に空きが出る。列幅の下限で列数を決める形は変えていない
    - **`/categories/` へは送らない。** あの一覧は運営用で、読者向けには作っていない。
      連載は `/series/` が読者向けにあるので、最後に「連載の一覧を見る」を置く
    - **上と下の分担は「網羅か抜粋か」ではなく「何の行き先か」**（#2952）。
      ヘッダーは**サイト内の記事を見つける軸**（カテゴリ全18件・連載）を持ち、
      リソースとアクティビティは各3件の抜粋。フッターは**リソースとアクティビティの
      全件**（各7件・計14件。うち8件が社外・別サイト）を持ち、**カテゴリと連載は
      持たない**。重なる5件が上と下の両方にあるのは意図（ヘッダーの
      「OSS / ツール」だけは組織ページを指すのでフッターに同じ行き先が無い）
      - 以前は「行き先の網羅はフッター4列が持つ」と書いていたが、フッターは
        記事へ届く2つの軸を持っていない。**フッターだけを見ても記事に届かない**
    - **検索窓のヒントパネルはタグだけになった**（#2877）。カテゴリと連載を
      ここが畳んで持つので、パネルに3種類並べる理由が無くなった。
      2か所に同じ行き先があるうえ、濃さとチップの形も揃っていなかった
      （#2871 / #2872 はこれで消える）
  - **帯の中は下線で反応しない。** hover は色を一段濃くするだけ（ナビ）か面を一段
    （ドロップダウンの行）で、サイト名は反応しない（フッターの `.footer-logo` と同じ）。
    所在は輪郭（`focus-visible`）だけが示す
    - **`:hover` だけ書くと Tab で来たときに青くなる。** 土台の `a:hover, a:focus` が
      `--a-color` と下線を当てていて、詳細度が 0,1,1 対 0,1,0 で土台が勝つ。
      帯の中のリンクは `:hover` と `:focus` の両方を書いて外す
  - **992px 未満ではナビが帯から降りて引き出しに入る。** 帯に並べると検索と重なるが、
    消してしまうとカテゴリと連載への行き先がモバイルから無くなる（フッター4列は
    リソースとアクティビティしか持たず、サイドバーのカテゴリは記事ページでは
    意図して伏せている）。**DOM は1つで、幅で横帯と縦の引き出しに切り替える**
    （同じ行き先を2度描かない）
    - 開閉はチェックボックスの `:checked` と兄弟結合子。JS は使わない
      （ランキングのタブと同じ手）。`input` は `.header-nav` より**前**に置く
    - **`label` に `tabindex` を付けて凌がない**（#2852）。`input` を
      「見えないがフォーカスできる」形（絶対配置＋`opacity: 0`）にして、
      Space で開閉する標準の振る舞いを使う。輪郭は `label` に描く
    - 名前は `label` の中の `sr-only` が持つ。絵しか無いと読み上げる名前が無い。
      開いているかは**チェックボックスの状態**として伝わるので、名前は
      「メニュー」のまま変えない
    - **引き金は三本線で、押すと ✕ に変わる**（#2883。リクルートサイトのモバイルと
      同じ仕様）。1枚の絵は動かせないので `svg-icon` の辞書は使わず、3本の span を
      並べて上下を ±135deg 回し、中央を消す。辞書に無い形を CSS で作るのは
      Feedly のマーク（mask）と同じ流儀。移動量の 6px は線の高さ2pxと間隔4pxの和で、
      間隔の段として選んだ値ではない。速さは開閉なので `tab-speed`
    - 引き出しの中では**畳みを二重にしない。** 4組は常に開いて並び、引き金は
      組のラベルとして残る（三角は出さない）
    - カテゴリの2列は**列数ではなく列幅の下限（190px）で決める**ので、
      引き出しに入る狭い幅では自動で1列に落ちる（幅375pxの端末が境目）
  - **検索の枠はフォーム1つだけが持つ。** 中の入力欄と送信ボタンは地も枠も持たず、
    反応（枠の色・地）もフォームの側だけに置く（1部品1反応・外側優先 #2686）。
    575px 以下では虫眼鏡だけにして、フォーカスされたときだけ**帯を覆って**広がる
    （入力欄だけを伸ばすとサイト名を押し出して帯が崩れる）
  - **広がった検索には ✕ を出す**（#2886）。畳む仕組みは「入力欄からフォーカスが
    外れる」だけなので、それ以外に閉じる手がかりが無かった。モバイルでは
    覆ったフォーム（64px）とヒントパネル（約250px）にソフトキーボード（約300px）が
    重なって、**外側をタップできる余白が50px前後しか残らない**
    - `✕` は**フォーカスを受けない `span`** にする。押せる部品にすると自分が
      フォーカスを取って `:focus-within` が続き、閉じない
    - キーボードの読者は Tab で外へ出れば閉じるので、`aria-hidden` でタブ順と
      読み上げから外す。**閉じる唯一の手段ではない**ので、#2852 の
      「label に tabindex を付けて凌ぐ」とは別の話
    - iOS は `cursor: pointer` の付いた要素をタップ対象として扱う
  - **全幅で覆う間は後ろの本文をぼかす**（#2888。Cloudflare と同じ形で、量も
    向こうの実測値の `blur(8px)`）。出るのは**モバイルの引き出しと、575px 以下で
    広がった検索の2つだけ。** PC の hover のドロップダウンには出さない。
    カーソルが乗っただけで本文全体がぼけるのは意図しない発火になる
    - 開いているかは **`:has()`** で拾う。引き金（`:checked` / `:focus-within`）が
      帯の中にあるので、兄弟結合子では帯の外の面まで届かない
    - **`position: fixed` にしない。** ヘッダーは追従しないので、開いたまま送られると
      引き出しは一緒に上へ動くのにぼかしだけ残る。帯の下から 100vh 分を
      絶対配置で敷く
    - 出し入れは `display`。`visibility` では隠れている間も 100vh の箱が残り、
      短いページに縦のスクロールが生まれる
  - **広がった検索の左右の余白は、フォームではなく中の部品（虫眼鏡・入力欄・✕）が
    持つ**（#2888）。フォームに `padding` を置くと、全幅で敷くパネルを同じ量だけ
    外へ出し戻す指定が要り、同じ値を2か所に書くことになる。パネルは端に接するので
    左右の枠と角丸を落とす
  - **ロゴはコーポレートの `logo-future.svg`**（`themes/future/source/`）。実寸
    257.99×81.92 のうち FUTURE の文字は下の31%で、上はクリムゾンの2本の線。
    28px で置くと文字はブログ名より小さく見えるが、それが正しい主従。
    外部ホスト（future.co.jp）を参照せずリポジトリに入れる
  - ホームでは `h1`、それ以外は `div`。**パンくずを1項目では出さない判断（#2837）は、
    ホームの h1 がここにあることを前提にしている**
- **フッターは全面ネイビー**（#2839）。グレー地＋ネイビーの円という以前の作りは捨てた。
  リンクの hover だけ下線が左から伸びる（0.25秒）。反応の速さは `hover-speed` の
  1つだけという規則の例外で、読者がわざわざ辿り着いた先なので動きを持たせても
  認知負荷にならない、という判断。**影と持ち上がりは入れない**（#2747 のまま）
  - `@import "_variables"` は `theme-styles.styl` の**先頭**にある（#2856）。
    以前は土台の塊より後ろにあり、それより前で変数を書くと解決されず
    `hover-speed` のような文字列がそのまま出力されていた（#2753 で踏んだ）。
    変数ファイルは CSS を1行も出さないので、先頭に移してもカスケードは変わらない
    （連結後の CSS が CSS 変数1つの宣言順を除いて同一であることを確認済み）
- **カテゴリ・タグ・著者ページの統計とグラフはサイドバーへ置く**（#2911）。
  これらのページに来た読者の目的は「その記事を読む」ことで、総シェア数の内訳も
  月別の投稿数の推移もその判断には効かない。`/tags/` `/categories/` はもともと
  裏画面で、読者向けになった後も運営向けの描画を本文の先頭に置いたままだった
  - **消さずに逃がすのは、運営と書き手がこの数字を見に来るため**（#2915 の
    「シェア数・反応の可視化は削らない」と同じ理由）。本文から抜いたぶん、
    **総本数は一覧の見出しの行の右端に「全N本」で出す**
    （`section-heading` の `meta`。2ページ目以降は `pagination-info` が同じ数を持つので渡さない）
  - **#2880 の「サイドバーは記事を見つける軸だけ」には抵触しない。** あれは
    **他の軸の一覧を並べない**という規則で、そのページ自身の数字はそれに当たらない
  - **置くのはサイドバーの一番上**（#2911）。**そのページについての数字**なので、
    別のカテゴリへ移る導線（カテゴリー一覧）より前に来る。そのカテゴリ／タグ／著者の
    ページに来た読者にとって、いま見ている集合の規模が先で、次の行き先はその後
  - **本文の統計のクラスをそのまま使い、並べ方だけ変える。** タイル（数値の下にラベル）を
    行（`265 投稿` と数値の右にラベル）に組み替えるだけで、`summary` / `summary-count` /
    `summary-label` / `summary-sub` と大きさの段は本文と共有する。**サイドバー用に
    新しいサイズを書かない。** 横に7タイル並べると 480px 要り、サイドバーの内寸
    （208 / 253 / 298px #2943）に入らないのが組み替えの理由で、値の設計は変わっていない
  - **主役は数値。** `summary-count` が 16px の太字、`summary-label` が 12px の通常。
    いちばん幅の要る「25599 総シェア数」で 113.2px なので、208px の列に 54% で収まる
  - **内訳が0の行は出さない**（#2911）。Pocket はサービス終了済みでこれから増えることが無く、
    終了後の記事だけで構成されるタグ・著者ではずっと「0 Pocket」が並ぶ。Facebook も
    1以上あるのは1,491件中93件だけで事情は同じ。**主要な行（投稿・著者数・総シェア数）は
    0でも出す**（そこが空くと数字の枠が何を数えているのか読めなくなる）
  - **情報の階層は本文のときと同じに保つ。** シェアの内訳（X / Facebook / はてブ / Pocket）は
    総シェア数の内訳で読者の行動を変える数字ではないので、`summary-sub` が数値を
    一段小さくする区別がそのまま効く（#2096）。**文字は `ink-faint` に落とす**
    （白地で 4.5 を満たす最も薄い段。これより薄くしない）
    - **内訳は字下げない**（#3004）。**数値は右端、ラベルは左端**で主要行とそろえる。
      内訳の数値だけが右へずれると **4989 + 253 + 10009 + 6528 = 21779 という
      足し合わせの関係が切れて**、**弱い項目が4つ並んでいる**ようにしか見えない。
      帳簿の内訳と同じ形で、従属は大きさ・色・線が示す
      - **数値の箱の幅は主要行と同じにする。** 桁で揃える幅は `ch` で持つが、
        `ch` は自分の `font-size` を見るので、一段小さい内訳では同じ桁数でも
        箱が狭くなり右端がそろわない（ずれ方は桁数で変わるので固定値では戻せない）。
        段の比（`text-body / text-meta`）で戻す。**ラベルの左端も自動的にそろう**
        （数値の箱の右に 6px で置いているだけなので）
    - **数値の段は `text-meta`**（#3004）。1つ隣の `text-row`（14px）では主要行との差が
      1.6px しかなく、**色だけで区別しているように見えた**。ラベルはどちらも
      `text-meta` で、下に段が無いので色と線が区別を担う
    - **さらに左に1本の線を引いて「1つの塊」にする。** 引用と同じ「塊の左に1本」
      （#2457）で、段は塊を囲う `rule-base`（区切りの `rule-weak` ではない）。
      行の間の空きを上下にまたいで1本につなぐ
  - **グラフの X 軸の目盛りは出さない**（`axisTick: { show: false }`）。月ごとに1本立つので、
    軸の月数が中央値80・最長128のここでは縦線の帯になって棒より目立つ。
    年ラベルは3〜4本しか出ないので目盛りと対応も付かない
  - **グラフの上は 24px 空ける。** 12px だと数字の枠と地続きに見え、枠の中の行間隔と
    区別が付かない
  - **枠は1つで、中に「累計」「直近1年」の2組を積む**（#3004）。同じページについての
    数字なので分ける理由が無く、枠を2つ置くと1つめの下辺と2つめの上辺で**線が2本並ぶ**。
    組の切れ目は間隔が作る（組の中の 4px・8px に対して 24px。#2747）。
    **枠を2つ並べていたときの間（22px）より狭くしない。** 線が無くなったぶん、
    切れ目は間隔だけが持つ
  - **節の見出しは置かない。** 「累計」「直近1年」のラベルと数字が並んでいれば何のブロックかは
    見て分かる。目で見えない読者には `<section aria-label="統計">` が名乗る。
    **時間軸のラベルを強めない。** 主役は数値で、箱の名前はその上に小さく添えるもの。
    本文の統計と同じ `summary-panel-label`（`ink-faint` × `text-meta`）のままにして、
    サイドバー用の上書きを持たない
    - **弱さは色の段が担う**（#3004）。`text-meta` が文字サイズの最下段なので、
      これ以上小さくはできない。段の外へ出す（`font_size_lint.mjs` の `EXEMPT` に
      登録する）ほどの理由は無く、1px 差は「見分けの付かない差に2つの役」に当たる
    - **名前と中身の間は間隔の刻み1つぶん（8px）。** 2px では数字の行間（4px）より
      狭く、箱の名前が1行目のラベルに見える
  - **グラフの凡例は出さない。** カテゴリ別の積み上げは最長の `DataEngineering` だけで
    117.9px あり、`type: scroll` の横送りになって場所だけ取る。どの色が何かは
    ツールチップ（`trigger: 'axis'`）が名乗る。高さは本文にあった 250px から 120px へ下げる
    （細い列で縦に長いと棒より余白が目立つ。軸ラベルに約20px 取られるので棒は 100px ぶん）
  - **軸ラベルは幅で自動的に間引かれる。** `chart-xaxis-year-labels` が
    `floor(幅 / 64)` で年ラベルの本数を決めるので、208px なら3本・298px なら4本になる。
    棒は軸の月数の中央値80ヶ月で 2.6〜3.7px/月と細い
  - **1024px 以下では出さない**（`display: none`）。サイドバーは本文の下に落ちるので、
    記事一覧25本とページャの後ろに数字を出しても読者は辿り着かない
    （目次を `.toc-mobile` に移したのと同じ理由 #2452）。
    総本数は一覧の見出しの「全N本」が幅に関係なく出す
  - **著者ページの表彰パネルとプロフィールは本文に残す。** 表彰は書き手にとっての
    意味が大きく、プロフィールは読者がその人を知る手がかり。逃がすのは
    累計・直近1年の数字と推移の3つだけ。この都合でプロフィールを h1 の直下に上げてある
  - **出すかどうかは呼び出し側が `showStats` で宣言する。** `page.author` は記事ページの
    フロントマターにもあり、`page.category` は「◯◯以外の記事」（自前の集計を持つ）にも
    あるので、ページの種類を `sidebar.ejs` の側で当てにしない
  - **echarts（gzip 309 KB）を読むのは1ページ目だけ。** カテゴリ18・タグ265・著者329 の
    612ページ。タグは記事5本未満だとグラフを出さない（#2434）
- **サイドバーは「記事を見つける軸」だけを置く**（#2880）。中身は人気のカテゴリ（8件・4群）→
  人気の連載（3件）→ 人気のタグ（10件）で、**粗い軸から細い軸への順**（18 → 78 → 714）。
  記事・タグ・著者・特設ページには出さない
  - **カテゴリだけはサイドバーで間引く**（#2908）。**上限8件**で、**全件はヘッダーの
    ドロップダウンが持つ**（サイドバーが唯一の導線ではない）
    - **上限は空間と認知から決める。分布の切れ目から決めない。** 切れ目で決めると、
      投稿の分布が変わるたびに上限の根拠がファイルの外へ出てしまう。
      空間の条件は**枠が3つあることが1画面で分かる**こと——1つ目の軸が画面を
      埋め切ると、残り2つは存在ごと隠れる。カテゴリ枠は y=118 から始まり、
      次の枠の見出しまでに 63px 要るので、**サイドバーが出るいちばん小さい画面**
      （1366×768、viewport 約640px）では枠の高さの上限が 459px
    - **いま効いているのは隣の枠との釣り合い。** サイドバーの3枠は
      人気の連載3件 → 人気のカテゴリ8件 → 人気のタグ10件で、粗い軸ほど
      1件の重みが大きいぶん件数を抑える。8件は連載とタグの間に収まる
      - 空間はもう上限を決めていない。**8件が空間から出た数なのは、群のラベルを
        出していた版の話**（ラベル4本の固定費155px ＋ 見出し33px ＋ 行242px ＝
        430px で、予算459px にぎりぎり収まる数が8だった）。群をやめた後は
        **275px で予算の6割**しか使っておらず、空間だけなら13件まで入る
      - 走査コストも群をやめた時点で「群の数＋最大群サイズ」では測れなくなった
        （平らな8行なので8）。増やすなら根拠を立て直す
    - **同じ「10件」でも枠によって値段が違う。** 人気のタグの10件は233px。
      カテゴリは行が縦に積むので、10件なら約335pxになる。件数を枠どうしで揃えない
    - 全件でないので**見出しが条件を名乗る**（「人気のカテゴリ」#2670）
  - **サイドバーは群を出さない。8件を平らに並べる。** 群は18件を2〜6行に落とす
    ための束ね方（#2908）で、8件まで絞った後は束ねる相手が2〜3件しか無く、
    ラベルのぶんだけ場所と手数が増える。**群は全件を出すヘッダーの
    ドロップダウンと `/categories/` が持ち続ける**
    - **並びは選ぶ鍵（直近1年のPV）と同じ順。** 群の並び（累計順）のまま群だけ
      消すと、画面から順序の根拠が消える（327 / 134 / 134 / 77 / 70 / 70 / 49 / 50 と
      単調にならない）。隣の人気の連載・人気のタグも自分の物差し順なので、
      3枠の読み方がそろう
    - **行にカテゴリのアイコンを添える。** 群が無くなったぶん、行の左端に
      識別の手がかりが要る。絵柄は `_partial/category-icon` が1箇所で持ち、
      ヘッダーのドロップダウン・パンくずと同じ
    - 群をサイドバーに出そうとして踏んだ順に、規則として残す価値があるのは3つ
      - **ラベルが行の列に自分の1行を持つと、目立つうえに行き先の列を群の数だけ
        分断する。** 出すなら左の余白へ（2列グリッド）
      - **群を表す手はその場所で1つだけ持つ。** 縦罫で範囲を示すならラベルの枠は
        外す。同じことを2つの部品が別々に言うことになる
      - 親と子の**どちらか片方だけ**に印を置く。行にアイコンがあって群が素の文字だと
        **子のほうが手厚く見える**
  - **カテゴリと連載の行は同じ形。** どちらも下線（`rule-weak`）で区切り、
    上下 10px・左 9px。1つのルールで持つ
  - **件数は 8px の `ink-faint`**（`.sidebar-count`）。括弧は付けない。
    ヘッダーのドロップダウンの `.header-dropdown-count` と**同じ数字**（その語の総数）
    なので同じ形にし、値は1つのルールから引く
    - **連載の行には出さない。** 連載名は数字で終わるものが多く
      （`秋のブログ週間2025` / `Go1.26リリース` / `PostgreSQL18リリース`）、
      後ろに素の数字を置くと名前の一部と見分けが付かない。
      括弧が無いぶん、この衝突は避けられない。総数は `title` が持つ
  - **軸の枠が出るのはホームと一覧ページだけ**（#2911 / #3024）。**カテゴリ・タグ・著者の
    ページでは3枠とも出さない。** そこに来た読者は既にその軸で絞り込んでいて、
    別の軸を並べても意図がぼやける。**カテゴリページで「人気のカテゴリ」も出さないのは、
    いま見ているカテゴリを含む一覧がそこに並ぶと、絞り込みの結果と移動先の一覧が
    同じ面に重なるため**（#3024。以前は「別のカテゴリへ移る導線」として残していた）。
    別のカテゴリへ移る導線は**パンくず（ホーム > カテゴリ）とヘッダーのドロップダウン
    （全18件）**が持つ。これらのページのサイドバーは統計とグラフだけになる
  - **軸でないものは置かない。** Tech Cast とアドベントカレンダーは #2880 で外した。
    旬のものを見せたいときはホームの「特設」枠が担う
  - **「人気の」が付く枠の物差しは「いまよく読まれているか」で揃える**（#2855）。
    同じ面に並ぶ入口が別々の基準で選ばれていると、どれが「いま」を指しているのか
    読者には区別できない。**「投稿本数」のような別の軸は混ぜない**
    - **いまは式が2種類ある。** 人気のカテゴリ・人気のタグは**直近1年のPV**、
      人気の連載は**経過年ペナルティ**（#3001。窓を外して1本ずつ `1/(1+経過年²)` で
      割る）。連載だけ先に移したためで、**カテゴリとタグも同じ式へ寄せる余地がある**
      （窓の境目で顔ぶれが飛ばなくなる）。揃えるならまとめて動かす
    - 直近3本未満を外すのはカテゴリ・タグ共通で、単発のバズを勢いと取り違えないため
    - **同点は PV の次に直近1年の本数で決める**（カテゴリのみ）。GA4 の値は
      100 単位に丸められている（全1,499件が100の倍数）ので同点が構造的に出て、
      候補が17件しか無いカテゴリでは上限の線にちょうど並ぶ（Business と Culture が
      10,000PV）。名前順で決めると、落ちる理由が読者から見て何も無くなる。
      タグ・連載は候補が多く線上の同点が起きにくいので名前順のまま
  - **768px 以下では3枠とも消える**（`display: none`）。サイドバーは本文の下に落ちるので、
    一覧25本とページャの後ろに出しても読者は辿り着かない（目次を `.toc-mobile` に
    移したのと同じ理由 #2452）。モバイルの行き先はヘッダーの引き出しが持つ
  - 連載をここに入れたのは、646本（全記事の44%）を束ねる軸だから（#2892）。
    ホームの本文にあった「連載から探す」2枚はこれに置き換えた。
    **「連載がある」ことは新着一覧自体が名乗る**——1ページ目の25本のうち12本が連載所属で、
    7本はタイトルに「連載」と入っている
- **ホームの「特集」枠は旬で2枚選ぶ**（#2880 / #2892）。優先順は
  アドベントカレンダー（開催年が今年）→ Tech Cast（新エピソード）→ HTTF（開催中）→
  連載（最終更新が4ヶ月以内）→ ガイドライン。前3つが季節もので、常設は後ろから落ちる
  - **季節ものはデータの年が名乗る。** アドベントは `advent_calendar.yml`、HTTF は
    `source/_data/httf.yml` の先頭の `year` を見る。年1回そこへ1行足す作業が
    そのままスイッチになるので、シーズンの終わりを別に持たなくてよい。
    **HTTF の回の年は開催年の翌年**（2026年の回は2025年11月開催）なので、
    条件は「今年と一致」ではなく「今年より先」。年が追いつくとオフシーズンになる
  - 以前は HTTF が無条件に入っていて、オフシーズンでも枠を1つ取っていた（#2892）
  - **連載を常設より前に入れるのは、旬のものが1枚も無い期間を無くすため。** 以前は
    オフシーズンに HTTF ＋ ガイドラインの常設2枚だけが残り、「旬で選ぶ」枠が名前どおりに
    働いていなかった。窓が4ヶ月なのは、**過去24ヶ月のどの月でも候補が1本以上あった**下限
    （3ヶ月に狭めると空く月が出る）
  - 見出しが「特設ページ」ではなく「特集」なのは、連載の索引が記事だから。
    連載も特設ページも「記事1本ではない、まとまった読み物への入口」で括れる
  - **枠には種別の違うものが混ざるので、1枚ずつラベルで名乗らせる**（`.panel-label`。#2892）。
    語彙は**ヘッダーのナビとフッターの列が既に持っているものだけ**——連載 / アクティビティ
    （アドベントカレンダー・Tech Cast・HTTF）/ リソース（ガイドライン）。ここで新しい
    分類語を作ると、同じものが場所によって違う名前で呼ばれる
    - **形はアイコン＋太字の文字で、チップにしない。** 丸い枠はサイト全体で
      **タグの印**になっていて、別の意味に流用すると「押せば絞り込める」と読まれる。
      **見た目の部品を使い回すことと、認知の負荷が下がることは別**で、
      想像と実体がズレるぶん流用の方が高くつく
      - 例外は表彰チップ（ベストブロガー等）だけ。文字数も装飾もタグと大きく違い、
        取り違えようがない
      - アイコンは `svg-icon` の辞書から。**連載は `/series/` の h1 と同じ `run-baton`** で、
        同じものに同じ絵を当てる。アクティビティ（`speakerphone`）とリソース（`book`）は
        #2892 で足した。人を表す `users` / `users-group` は著者一覧と Management
        カテゴリが使っているので、アクティビティには当てない
      - 太字なのは、すぐ下の `.panel-meta` が同じ `ink-mute` で、役の違いが
        大きさでは読み取れないため（どちらも `text-meta` #2971）
    - ラベルはリンクの**外**に置く。種別はカードの属性であって導線ではなく、
      同じ行き先のリンクを1枚のカードに2つ置かない (#2845) 決まりにも合う
    - **ラベルが名乗るぶん、説明文からは種別を落とす**（連載の `全9本・2026.08 更新` に
      「連載」と書かない）。同じことを2回言わない
  - **「◯◯一覧へ」は種別ごとの導線なので、置ける場所が中身で変わる**（#2892）。
    2枚とも特設の回は行き先が1つなので**節の下**にまとめて置く。連載と特設が混ざる回は
    行き先が2つになるので、**それぞれのパネルの中**に入れる。節の下に「特設ページ一覧へ」
    だけを置くと、隣に連載が並んでいるときに枠全体の導線に見えて嘘になる
- **記事末の自動生成ブロックは全幅の帯で切る**（#2874）。関連記事・被参照・We're hiring は
  本文への従属が弱いので、本文に従属するもの（連載ナビ・共有ボタン・著者紹介）と面で分ける。
  `<article>` の外に出し（`_partial/article-appendix.ejs`。`post.ejs` の `container` の外で描く）、
  画面の全幅に `surface-tint` を敷いてフッターのネイビーに接する。左右の揃いは中の
  `.container` が持つ。全幅なので角丸は持たない
  - **空きでは分けられない。** 境目はもともと 67.5px（著者紹介がある記事は 48px）空いていて、
    本文の中の節の切れ目（56px）より広いのに地続きに見えていた。量ではなく質を変える
  - **中身は白の面に戻す**（`.article-appendix-inner`。角丸 `card-radius`）。行の細線
    （`rule-weak` 1.19 → 1.09）・行の hover（`surface-mute` 1.16 → 1.06）・NEW の枠・空サムネは
    いずれも白地の上で決めた値で、グレーの上では一段弱くなる（#2732 で確認した性質）。
    **グレーは領域の枠として外側に使い、読む対象は白の上に置く**
  - **見出しの大きさは動かさない。** 面が本文との違いを持つので、大きさまで動かすと
    面と大きさの両方を動かすことになる（#2722）。帯は `<article>` の外のページ単位の領域なので、
    節見出しは一覧ページと同じ `section-heading`（24.05px）を使う。18.2px に下げる案は
    面を入れた時点で要らなくなった。We're hiring の罫線（`.career-counseling` の
    `.article-entry` から降りていた）は `.bottom-content-header` 側で外す。罫線は本文の h2 だけが持つ印
  - **目次には入らない**（`toc(post.content)` は本文だけ）。`aside` に `aria-label` を付けて
    complementary landmark にし、見出しで飛ばす読者が帯をまとめて飛ばせるようにする
  - 帯までの空きは `.article-appendix` の `margin-top` の1箇所で決める。共有ボタンの下 padding と
    著者紹介の `margin-bottom` を落として、著者紹介の有無で空きが変わらないようにした
    （以前は 48px と 67.5px でばらついていた）
  - 短いページでは `.wrap > footer` の `margin-top: auto` が帯とネイビーの間に白を入れるので、
    余りは帯より前（本文の `container`）で吸わせる
  - 残っている難点: 中身がコンテナ幅（約1,116px）になるぶん、関連記事の行のタイトル占有率が
    45% → 31% に落ちる（全4,447行の中央値。折り返す行は 1.2% → 0%）。日付・♡ を行の右端へ
    寄せれば埋まるが、ランキング・関連・被参照は同じ部品（`postListItem`）で揃えているので
    ホームのランキングにも及ぶ。別に判断する
- **記事ヘッダーのメタ情報は2段**（#2907）。1段目（`.blog-info`）が
  **日付・著者・文字数・PV**、2段目（`.article-header-tags`）が**タグ**。
  カテゴリはパンくずが持つ（#2837）
  - **タグをタイトルの近くに置くのは、そこが読者の探し方に合う場所だから。**
    Cloudflare Blog も Zenn もタイトルの近くに置いている。記事末へ移す案は
    **共有ボタン・関連記事・被参照記事と役が重なる**ので採らない（#2907 の
    当初案。PR #2955 は Draft のまま閉じた）——読者は共有ボタンで反応を返した後に
    タグで次を探すが、その「次を探す」は関連記事・被参照記事が既に担っている
  - **タグを2段目に落とすのは、タイトルと重なる情報だから。** 冒頭で効くのは
    「いつ・誰が・どれだけ」の方なので、そちらを1段目に置く
  - 2段の間は 12px（`.blog-info` の行間と同じ）。**本文までの空きは変えない**
    ——`.blog-info` が持っていた下の空き（margin 20px + padding 8px）を
    `:has(+ .article-header-tags)` で消してタグの下へ送る
  - **チップの文字の左端を本文に揃える**（`margin-left: -9px` ＝ 枠1px + 左パディング8px）。
    揃えるのは枠ではなく文字の位置（#2429 / #2453 と同じ規則）
  - 見出しは付けない。チップの形がサイト全体で「タグの印」なので、「タグ」の1行は
    場所を取るだけになる（#2892 / #2567）
  - 狭い画面（767px 以下）ではメタ行が**2行**に折り返す。1行目「いつ・誰が」、
    2行目「どれだけ書かれ、読まれたか」。タグはその下の段が持つ
- **パンくずは道筋だけを出す**（#2837）。規則は3つ。
  ①道筋（上位階層）はリンクとして全部出す
  ②**現在地は出さない。真下の h1 が名乗るから。** ただし h1 が名乗らない絞り込み
  （`Nページ目`）だけは現在地として残す
  ③**2項目未満になるならパンくず自体を出さない**（`_partial/breadcrumb.ejs` が
  `items.length >= 2` で全体を囲っている）
  - 出ないのは9ページ種（ホーム1ページ目 / カテゴリ・タグ・著者・連載の各一覧 /
    すべての記事 / 特設トップ / Tech Cast / 記事ドクター）。h1 はその分 26.4px 上がるが、
    **空きを足して揃え直さない。** 中身の無い帯を場所取りに使うと③と矛盾する
  - **①には記事ページだけ例外がある。中間の「カテゴリ一覧」を飛ばし、その記事の
    カテゴリへ直接返す**（`ホーム > Programming カテゴリ`）。記事はメインのコンテンツで
    SNS からの入口になり、そこにいる読者が次に行きたいのは同じカテゴリの記事であって
    「カテゴリ全部」ではない。カテゴリ一覧への導線は「他のカテゴリは？」となる位置＝
    カテゴリページのパンくず（`ホーム > カテゴリ`）が持つ
    - ラベルに「カテゴリ」を足すのは、直前の項目が種別を名乗らなくなるため。
      行き先のページの h1 と同じ文字列になる
    - **カテゴリは記事のメタ行（`.blog-info`）から外した。** パンくずと二重になるため。
      アイコン（`_partial/category-icon`）もパンくず側へ移してある。
      **パンくずにアイコンを添えるのは記事ページだけ**で、一覧・期間・著者は素のテキスト。
      一覧カード（`_partial/archive-post.ejs`）にはカテゴリを残す。同じページに複数の
      記事が並び、そのページのパンくずは個々の記事のカテゴリを指さないため
    - アイコンは描画済みの SVG を `items[].icon` で渡す。絵柄の辞書は
      `_partial/svg-icon.ejs` が1箇所で持つ。`itemprop="name"` の外に置いて、
      機械に渡す名前は文字だけに保つ
    - **一覧・期間・著者はメタなページなので道筋を全部出す**（`ホーム > すべての記事 > 2026年`、
      `ホーム > カテゴリ > Programming`）。技術的な知見を求める読者とは別の行動を取る人しか
      見ないので、階層を辿れる方が役に立つ。メイン（記事）とメタ（管理用の一覧）で扱いを分ける
  - 1項目のパンくずが仕事をしないのが理由。現在地は h1 が名乗り、ホームへの行き先は
    ヘッダーのサイト名リンク（全ページ固定）と同じで、機械にも
    「ホームへのリンク」しか宣言していない
  - 構造化データ上も問題ない。Google のパンくずの仕様は
    「トップレベルのパスにも、ページ自身にも `ListItem` は必須ではない」と書いている。
    落とす現在地はもともと `<a>` ではないので、**サイトから消える `<a>` は実質ゼロ**
- **パンくずの現在地は太さで示す**（#2705 / #2722）。`_partial/breadcrumb.ejs` は
  「url を持たない項目が現在地」という作りで、現在地は非リンクになる。
  以前はこの違いにスタイルが無く、リンクと同じ色・下線なしで並んでいた。
  #2837 以降、現在地を持つのは**ページャのある2ページ目以降だけ**
  - 現在地は**太字だけ**。色はリンクと同じ `ink-mute` で、区切りの `>` は `ink-faint`。
    リンクの hover は他の添えのリンクと同じ `ink-strong` ＋下線なので、
    **太字・下線・素の3つで現在地・hover 中のリンク・リンクが見分けられる**
    - **色は動かさない**（#2722）。#2705 では現在地を `ink-strong` に濃くしていたが、
      14px の帯の中で色と太さを同時に動かすと現在地だけ黒く浮いて見えた
  - **リンク側は太字にしない。** 「添えの情報のリンクは `ink-mute` ＋太字」（#2681 / #2699）の
    例外で、2〜4項目すべてが太いと帯が重くなり現在地の強調が消える。
    日付・著者名の太字は単独で置くメタ情報という違いがある
  - 現在地の `li` には `aria-current="page"` を付ける。目で見る読者・支援技術・機械
    （microdata の `itemprop="item"` の有無）の3方向に同じ違いを渡す

## コメント

- **他の場所の値と一致していることをコメントで主張しない**（#2768）。一致に意味が
  あるならコードで値を共有し、意味が無いなら書かない。グラフの色に「ページャの現在地と
  同じ #337ab7」と書かれていたが、#2681 でページャがネイビーへ移った後もこの記述が残り、
  #2767 の検討で存在しない論点を1つ立てることになった（`#337ab7` はその時点でグラフ2枚に
  しか残っていなかった）
  - **腐るのは値を名指しした形だけ。** 構造の話（「ホームと同じ形」「著者ページのバッジと
    同じ部品」）は相手が動いても意味が変わらないので対象外
  - 一致が意味を持つなら値を1箇所に置いて両方から参照する。グラフの色と線の太さは
    `scripts/chart_style.js`、CSS の色と段は `_variables.styl` が持つ
    （`theme-styles.styl` 冒頭の `:root` はそれを CSS 変数として配るだけ #2856）。
    サイズは `theme-styles.styl` の各ルールが1箇所ずつ持つ
  - この形だったインラインコードのリンク色は #2860 で `link-on-tint` にした。
    `theme-styles.styl` と `highlight.styl` の両方に書く必要は残る（詳細度の都合）が、
    値を1箇所から引くので**揃っていることを説明するコメントが要らなくなった**。
    値の重複を消せば主張も消える、という形の例
- **色は `_variables.styl` が値を持ち、`:root` が CSS 変数として配り、使う側は `var()` で引く**
  （#2746。ダークモードの前段）。3つの役を分ける
  - **値は Stylus 変数のまま置く。`:root` にだけ書かない。** CSS カスタムプロパティは
    **計算に使えない**ので、スタイルガイドが `luminosity()` からコントラスト比を出せなくなる（#2858）
  - **Stylus の関数に渡すところは `var()` にしない。** `code-bg(surface-tint)` のように
    色を計算に使う箇所は Stylus 変数のまま（`hsl()` が `var()` を受け取れない）。
    いま該当するのは `.article-entry th code` の1箇所
  - スタイルガイドのマップ（`sg-on-white` ほか）も Stylus 変数のまま。見本の色見は
    `var()` になるので、ダークでは**見本が暗い値・16進数が明るい値**になる。そこは別に直す
  - **`#fff` には2つの役がある**（#2746）。**白い面は `surface-base`** で、ダークでは反転する。
    **濃い面の上の白い文字は `on-dark-strong`** で、ダークでも白のまま。
    30件を数えると 14 対 14 に割れた（残る2件はスタイルガイドの計算の引数）
  - **表彰の淡金は `award-*` の6トークン**（#2742 でブランド色を表彰に広げないと決めたので、
    この系統だけが持つ色）。金が2つあるのは地が違うため。`award-gold-on-tint` は
    淡金の地で AA を満たす濃さで、白地のバッジ用の `award-gold` は淡金の上では 4.19 と割る
  - **コメントの真偽は機械では検査できない。** hook は lint を起動する引き金にすぎない。
    検査できるのは値の重複の方で、色リテラルを許可リスト方式にすれば
    「宣言されていない色が1〜2箇所にだけ残っている」状態は拾える。対象が
    `theme-styles.styl` 全体に及ぶので未着手

## その他

- URL の付け替え（タグ→カテゴリの統合、タグの名寄せ）は `_config.yml` の `alias` に記述する
- **CSS だけのタブは radio をフォーカス対象にする**（#2852）。`label` に `tabindex` を
  付けて凌がない。label はクリックのときだけ radio へ転送する要素で、**Enter でも
  Space でも発火しない**ため、フォーカスは当たるのに切り替えられない状態になる。
  radio を `display: none` ではなく「見えないがフォーカスできる」形（絶対配置＋
  `opacity: 0`）にすると、ブラウザのラジオグループの振る舞い（Tab で組に入り、
  矢印キーで移動しながら選択、Tab で抜ける）がそのまま効く
  - 輪郭は見えない radio ではなく label に描く（`input:focus-visible + .tab_item`）
  - **Enter では切り替わらない。** ラジオの標準キーではなく、ARIA のタブの作法でも
    矢印キーが本線なので、そこは合わせる
  - タブ停止は1グループ1つになる（著者ページの年タブは12→1）
  - **矢印が使えることは `.tab-hint` が名乗る。** 支援技術にはラジオとして伝わるが
    （`role=radio` / `checked`）、目で見るキーボード利用者には手がかりが無い。
    「← → で切り替え」を**フォーカスしているタブの真下**（本体の上の余白 18px の中）に
    絶対配置で出す。レイアウトは動かず、マウスの読者には一度も出ない
    - **タブから離すと気づかれない。** 帯の右端では、ホームの帯で最後のタブから
      700px 離れる。帯の上の余白（20px）は見出しが左半分を占めていて左に寄せられない
    - **文字は `label` の中の span が持つ**（`_partial/tab-hint.ejs` が1箇所で持ち、
      各ラベルが差し込む）。`::after` の生成文字列にすると**ラジオの読み上げ名に混ざる**
      （「トレンド ← → で切り替え」になった）。span 側は `aria-hidden` で外す
    - **色は輪郭と同じネイビー。** 同時に出るものを1つの信号として読ませる。
      差し色（クリムゾン）は使わない。役が3つ（ランキング1位・フッターの波・
      フッターの hover）あるうえ、白地の対比は 5.35 で ink-mute の 6.19 より低く、
      12px の文字では**目立つが読みにくい**（ネイビーは 16.34）
    - **992px 未満では出さない。** 著者ページの年タブが折り返し、真下の文字が
      2行目のタブに重なる。矢印キーを使う読者はキーボードのある画面幅にいる
  - **帯そのものにも名前を付ける**（`role="radiogroup"` ＋ `aria-label`）。
    ラジオとしては伝わるが、名前が無いと「何の切り替えか」が読み上げられない
- **キーボードの入口はスキップリンク**（#2846）。Tab の起点を持つのはブラウザで、
  ホイールで送っただけでは動かない（クリックすると動く）。そのため読者が
  ページのどこを見ていても Tab は先頭の検索窓に入り、その focus でヒントの
  パネルが開いて**本文まで25ストップ**（うち23はパネルのカテゴリ・タグ）かかっていた
  （#2877 でパネルはタグ10件だけになり、代わりにヘッダーのナビが4つのタブ停止を持つ）。
  `layout.ejs` の先頭に「本文へスキップ」を置き、各レイアウトの `#main` へ飛ばす
  - **新しいレイアウトを足したら `id="main" tabindex="-1"` を付ける。**
    `tabindex` が無いとフォーカスが本文へ移らず、次の Tab がまた先頭から始まる
  - **その要素は `<main>` にする**（#2925）。ランドマークで移動する読者にとって
    本文の範囲を示すのがこれで、`<main>` は1ページ1つなので入れ物を分ける理由が無い。
    `<main>` は **h1 から包む。** 統計・グラフだけがランドマークの外に残ると、
    「本文はどこからか」が読者と機械で食い違う
    - サイドバー（`<aside>`）は `<main>` の**兄弟**に置く。本文ではないため
    - パンくずは `<main>` の外なので、`_partial/breadcrumb.ejs` 側が
      `<nav aria-label="パンくず">` で自前のランドマークになる
    - **全18ページ種がこの形**（#2953）。以前は `<main>` と `#main` を別要素に
      分けた形が9本あり、着地点が `<main>` の内側（8本）と外側（1本＝記事ページ）に
      割れていた。余白のための入れ物は `<div class="margin-top-30">` として残っている
      （`<section>` のままだと見出しを持たない節に見える）
    - 記事ページの `id="main"` は `_partial/article.ejs` の `<main>` が持つ。
      **この partial は1ページに1回しか描かない**（`_partial/archive.ejs` の
      `pagination == 2` の枝がループで呼ぶ形だが、`config.archive` が undefined で通らない）
  - `position: fixed` にする。絶対配置だと押される前にブラウザがページを
    先頭まで送ってしまい、読んでいた位置を失う
  - 検索パネルのリンクをタブ順から外す案は採らない。キーボードだけの読者が
    そのタグへ到達できなくなる。ナビのドロップダウンも同じ考えで、
    閉じている間は `visibility: hidden` なのでタブ順に出ず、
    引き金にフォーカスが当たると開いて中へ入れる
- **同じ行き先のリンクを1つのカードに2つ置かない**（#2845）。サムネイルとタイトルが
  並ぶカード（一覧・連載/特設パネル・We're hiring・HTTF）は、サムネの `<a>` に
  `tabindex="-1"` と `aria-hidden="true"` を付けてタブ順と読み上げから外す。
  マウスでは今までどおり画像を押せて、キーボードと支援技術ではタイトルの1回だけ止まる
  - 一覧は1ページ25件なので、付け忘れると1ページで25回の空振りが増える
  - サムネの `<img>` は `alt=""`。名乗るのは隣のテキストで、両方が名乗ると二重になる
  - 部品ごとに逃がさない。`.thumb-link` を使うリンク8か所と、関連記事・ランキングの
    `post-list-icon`（`scripts/lib/post_list.js`）が同じ形。先にこの形だったのは後者で、
    #2845 で残りを揃えた
- **外部リンクの明示（#2346 / #2729）**: サイトの導線（フッター・パネル・特設等）で、
  リンクテキストから外部と読めないものだけに印を付ける。ブランド名を名乗るリンク
  （connpass・note・YouTube 等）と記事本文には付けない。
  - **印は矢印（`external-link` アイコン）＋読み上げ用の `<span class="sr-only">（外部サイト）</span>`**。
    `svg-icon` に `class_name: 'svg-icon-trailing'` を渡して文字の後ろに置く。
    #2346 では「アイコンは導入せずテキストで書く」としていたが、フッター刷新（#2664）で
    アイコン方式が入り、テキスト・アイコン・見出しに書いたサービス名の3通りが混在していた。
    フッターの形に統一し、**見出しからサービス名を外した**
    （「アドベントカレンダー（Qiita）」→「アドベントカレンダー」）。
    見出しに行き先を書くと、枠の名前と行き先の説明が1行に混ざって読みにくい
  - **リンクを組み立てるのは EJS 側に置く。** アイコンは `_partial/svg-icon.ejs` の辞書が
    1箇所で持つので、JS の中で HTML を作ると絵柄を二重に持つことになる。
    Tech Cast は helper（`techcast_items`）がデータだけ返し、
    `_widget/techcast.ejs` が描く形にした。textlint から見える場所にリンクが来るのも利点
  - **名乗り方は場所で決まる。例外は作らない**（#2652 / #2729）

    | 場所 | 名乗り方 |
    | --- | --- |
    | **フッター** | **テキストを持つ外部リンクは全部に矢印＋`sr-only`**（#2839） |
    | サイドバーの枠と記事末のパネル | リンクごとに矢印＋`sr-only`。ブランド名を名乗るものは付けない |
    | 読み物（記事・特設ページ**すべて**） | **ページの本文で一度名乗る。リンクに印は付けない** |

    フッターだけ「ブランド名を名乗るものも例外にしない」のは、行き先が社外・
    別サイトに散っていて、印の有無が語彙の差ではなく抜け漏れに見えるため（#2839）。
    アイコンだけのリンク（X / GitHub / Feedly / RSS）は `aria-label` が名乗るので
    対象外で、これは textlint のルールも同じ扱い

    読み物の中の参照リンクに毎回印が挟まると読みにくい（記事本文に付けない判断＝
    技術記事の参照は94%が外部、と同じ理由）。特設ページは本文が行き先を名乗る形に揃えた
    （「Qiita Advent Calendar に参加しています」「2018年から毎年 AtCoder…」
    「いずれもこのブログとは別のサイトで公開しており」）
    - **記述言語（EJS か Markdown か）では決めない。** #2652 ではその線で切っていたが、
      EJS の特設ページには導線と読み物の両方があり、実態と合わなかった
    - `/specials/guidelines/` は最後まで例外にする案もあった。3つの実体は
      **同じホストの別リポジトリ**（`future-architect.github.io/arch-guidelines/` 等）で、
      ドメインが変わらないぶん見分けが付きにくい。それでも「読み物は本文で名乗る」に
      揃えたのは、例外を1つ持つと次にどちらへ寄せるかの判断ができなくなるため
    - 読み物として扱う EJS のページは `.textlintrc` の `exemptFiles` に挙げる
      （`layout/advent-calendar.ejs` / `layout/httf.ejs` / `layout/guidelines.ejs`）
  - 漏れは **textlint の `no-unmarked-external-link`** が止める。`/doctor/` にあった
    同じ検査は消した（hexo generate して運営ページを開かないと気づけず、PR では誰も見なかった）。
    許容リストは `.textlint/textlint-rule-no-unmarked-external-link/exempt.js`。
    ルールは `<a>` の中のテキストを見るので、`sr-only` に書いた「（外部サイト）」でも通る
    - **実際に検査できるのは `href` に URL を直書きしたリンクだけ**（#2729）。プラグインの
      正規表現は `href="https://…"` を要求し、EJS タグは空白に置き換えられるので、
      `href="<%= c.url %>"` の動的リンクは1件もマッチしない。**フッターとポータルは検査され、
      サイドバーの枠・記事末のパネル・特設の表は検査されていない**。動的な href まで見るには
      「URL が外部かどうかを判定せずリンクテキストだけを見る」形にする必要があり、
      内部リンク（`url_for` 由来）まで拾って誤検知が増える
  - `--fix` は持たない。印を付ける位置（リンクテキストの末尾）は決まったが、
    「そのリンクテキストから外部と読めるか」は機械には判断できない
  - **パネルのラベルは JS がデータを持ち、印は EJS が付ける**（`scripts/hiring_panels.js` の
    `external: true` を `_partial/article.ejs` が見る）。JS の中の文字列は textlint から
    読めないので、印そのものを JS に書くと検査の外に出てしまう
- **出版・寄稿の一覧は記事のフロントマターから作る**（#2912。`/specials/publications/`）。
  `books:` と `magazines:` を出版タグの記事に書き、`scripts/publications.js` が集める。
  マスタを別ファイルに持たないので、書いた本人が記事を書くときに一緒に書ける。
  退職しても「出版時に在籍していた社員の記録」として残る
  - **本文中のリンクからは拾えない**（issue の当初案）。出版社・書店のドメインへのリンク
    57件を実際に見ると、**自社の本と参考に挙げた他者の本が同じ形で混ざる**
    （「日本語書き下ろしで3版まで進んでいるのは Sphinxをはじめようと、本書のみ」・
    「後藤ゆきさん著の細かく紹介している本」）。どちらかは本文を読まないと分からない
  - **まとめる単位は号ではなく寄稿1件。** 1つの号に複数の寄稿がある
    （2019年3月号は機械学習の章とJavaScriptの章の2本で、書いた人も違う）
  - **並べるのは記事の日付ではなく「出たもの」の順。** 号の年月は URL の
    `/archive/YYYY/YYYYMM` から取る（2024年8月号の記事が12月に出ていた）。
    巻で数える WEB+DB PRESS は年までしか取れないのでその年の最後に置く。
    **取れる行と取れない行で基準を変えると比較が一貫せず、並びが崩れる**
    （一度踏んだ）。全部を YYYYMM の数値1つに寄せてから比べる
  - **表紙は並べない。** 買い物ページではなく「誰が何を書いたか」の記録なので、
    絵よりも書名・寄稿タイトル・執筆者が読めることを優先する
  - **記事の無い書籍は載らない。** 「記事に付随して書く」ことの帰結。
    **ページ本文にはそう書かない**（#2895。読者に語るのは中身であって、
    ページの作り方と収録の運用ではない）
  - 書き方は `.claude/skills/publish-qiita/SKILL.md` の「フロントマター整備」が持つ。
    **タグに `出版` を付けるだけでは載らない**ので、公開手順の側に書いておく
- **特設・固定ページはルート直下に置かず `/specials/` 配下に切る**（#2344）。
  GitHub Pages のプロジェクトサイトが `future-architect.github.io/<リポジトリ名>/` に生えるため、
  ルート直下のパスは将来のリポジトリ名と衝突しうる（/arch-guidelines/ 等は既に別リポジトリが占有）。
  `tags/` `categories/` 等の既存コア機能パスは既得として維持する
- 画像圧縮は pngquant / jpegoptim（`jpegoptimall.bat`）を月次で回す運用。詳細は README.md
